# BP 2026 Parte 4 (CAC, SG&A, Bônus, EBITDA) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar (−) CAC, (−) SG&A (com complemento do Caju), (−) Bônus e (=) EBITDA à matriz de `/bp-2026`, refatorando antes os maps/queries duplicados.

**Architecture:** Refactor sem mudança de comportamento (helper de query de despesa-caixa + builder de linhas), depois a 4ª família de linhas pelo caminho novo. EBITDA derivado de `margemMeses` via `subtrairMeses`.

**Tech Stack:** igual às partes anteriores.

**Spec:** `docs/superpowers/specs/2026-06-10-bp2026-parte4-ebitda-design.md`

**Contexto:** worktree `/Users/mac0267/Cortex/.claude/worktrees/bp2026-orcado-realizado`, branch `feature/bp2026-orcado-realizado` (PR #247). Xlsx só em `/Users/mac0267/Cortex/`. Senha prod via `.env` (`PROD_PASS=$(grep -E "^# *DB_PASSWORD=" .env | head -1 | sed -E 's/^# *DB_PASSWORD=//' | tr -d '"'"'"' \r')`). Referências (prod, caixa): CAC jan=238.227, fev=235.910, mar=304.046, abr=496.949, mai=334.622; SG&A-sem-benefício jan=189.823, fev=158.670, mar=186.545, abr=190.787, mai=203.058; Bônus(05.01.10) jan=51.664, fev=21.342, mar=21.648, abr=3.300, mai=6.224. Orçado: cac jan=262.495,7; sga jan=227.706; bonus jan=54.000/fev=23.000/mar=23.000/resto 0; EBITDA orçado jan=238.731,27 (=782.932,97−262.495,7−227.706−54.000).

---

### Task 1: Seed — cac, sga, bonus (com células vazias)

**Files:**
- Modify: `scripts/seed-bp2026-orcado.py`

- [ ] **Step 1: Estender o script.** Adicionar a `LINHAS` (após a entrada de csv_stack, mantendo a do SG&A/benefício no fim ou em qualquer ordem — a lista é (aba, linha, metrica)):

```python
    ("Overview", 15, "cac"),
    ("Overview", 16, "sga"),
    ("Overview", 17, "bonus"),
```

A `TOTAIS_ESPERADOS`, adicionar:

```python
    "cac": 4449113.7,
    "sga": 2688672.0,
    "bonus": 100000.0,
```

Após `TOTAIS_ESPERADOS`, adicionar:

```python
# Métricas com células vazias na planilha (tratadas como 0). Ex.: Bônus só tem jan-mar.
PERMITE_VAZIO = {"bonus"}
```

No loop, logo após `valores = [...]`, inserir:

```python
    if metrica in PERMITE_VAZIO:
        valores = [v if isinstance(v, (int, float)) else 0 for v in valores]
```

(o assert de células numéricas continua logo depois, agora satisfeito para bonus; estrito para as demais.)

- [ ] **Step 2: Rodar e aplicar nos dois bancos**

```bash
cd /Users/mac0267/Cortex && python3 /Users/mac0267/Cortex/.claude/worktrees/bp2026-orcado-realizado/scripts/seed-bp2026-orcado.py
PGPASSWORD=dev123 psql -h localhost -U cortex -d cortex_dev -f /tmp/seed-bp2026-orcado.sql
PROD_PASS=$(grep -E "^# *DB_PASSWORD=" .env | head -1 | sed -E 's/^# *DB_PASSWORD=//' | tr -d '"'"'"' \r') && PGPASSWORD="$PROD_PASS" psql -h 34.95.249.110 -U postgres -d dados_turbo -f /tmp/seed-bp2026-orcado.sql
```

Expected: `OK: 156 statements` (12 DELETEs + 144 INSERTs), `COMMIT` nos dois.

- [ ] **Step 3: Verificar** — query de grupo nos dois bancos (12 métricas; novas: cac 12 4449114, sga 12 2688672, bonus 12 100000). Spot-check: `SELECT mes, valor FROM cortex_core.bp2026_orcado WHERE metrica='bonus' ORDER BY mes;` → 54000, 23000, 23000, depois 0 até dezembro.

- [ ] **Step 4: Commit**

```bash
git add scripts/seed-bp2026-orcado.py
git commit -m "feat(bp2026): seed de CAC, SG&A e bônus (células vazias = 0)"
```

---

### Task 2: Refactor sem mudança de comportamento (API)

**Files:**
- Modify: `server/routes/bp2026.ts`

- [ ] **Step 1: Capturar payload de referência ANTES do refactor.** Criar `bp2026-smoke.ts` na raiz (NÃO commitar; manter até o fim da Task 3):

```typescript
import "dotenv/config";
import express from "express";
import { registerBp2026Routes } from "./server/routes/bp2026";
import { db } from "./server/db";
import { writeFileSync } from "fs";
const app = express();
registerBp2026Routes(app, db);
const server = app.listen(3992, async () => {
  const res = await fetch("http://localhost:3992/api/bp2026/receitas");
  const json: any = await res.json();
  delete json.atualizadoEm;
  writeFileSync(process.argv[2] ?? "/tmp/bp2026-payload.json", JSON.stringify(json, null, 1));
  console.log("payload salvo:", process.argv[2] ?? "/tmp/bp2026-payload.json", "linhas:", json.linhas.length);
  server.close(); process.exit(0);
});
```

Run: `npx tsx ./bp2026-smoke.ts /tmp/bp2026-antes.json` → `linhas: 11`.

- [ ] **Step 2: Helper de query.** Em `server/routes/bp2026.ts`, antes de `registerBp2026Routes`, adicionar:

```typescript
// Soma mensal de despesas em regime caixa (QUITADO por data_quitacao em 2026),
// filtrada por um predicado de categorias.
async function somaDespesaCaixaPorMes(
  db: any,
  predicadoCategorias: ReturnType<typeof sql>
): Promise<Record<number, number>> {
  const result = await db.execute(sql`
    SELECT EXTRACT(MONTH FROM data_quitacao)::int AS mes,
           SUM(COALESCE(valor_pago::numeric, 0)) AS total
    FROM "Conta Azul".caz_parcelas
    WHERE tipo_evento = 'DESPESA'
      AND status = 'QUITADO'
      AND data_quitacao >= '2026-01-01' AND data_quitacao < '2027-01-01'
      AND (${predicadoCategorias})
    GROUP BY 1 ORDER BY 1
  `);
  const porMes: Record<number, number> = {};
  for (const row of result.rows as any[]) {
    porMes[Number(row.mes)] = parseFloat(row.total);
  }
  return porMes;
}
```

Substituir os 4 blocos existentes de query de despesa-caixa (4c impostos, 4d salários, 4e benefício, 4f stack) por chamadas ao helper, mantendo os comentários numerados:

```typescript
      // 4c. Impostos sobre receita: regime caixa (quitação), categorias 05.05.x
      const impostosPorMes = await somaDespesaCaixaPorMes(db, sql`categoria_nome LIKE '05.05%'`);

      // 4d. CSV Salários: caixa — folha de operação (05.01 exceto premiações) + freelancers (05.02)
      const salariosPorMes = await somaDespesaCaixaPorMes(
        db,
        sql`(categoria_nome LIKE '05.01%' AND categoria_nome NOT LIKE '05.01.10%') OR categoria_nome LIKE '05.02%'`
      );

      // 4e. Benefícios totais da empresa (Caju): caixa — rateado depois pela fração orçada
      const beneficioTotalPorMes = await somaDespesaCaixaPorMes(db, sql`categoria_nome LIKE '06.10.04%'`);

      // 4f. CSV Stack: caixa — todo o software da empresa (conceito da planilha)
      const stackPorMes = await somaDespesaCaixaPorMes(
        db,
        sql`categoria_nome LIKE '05.03%' OR categoria_nome LIKE '05.04.01%' OR categoria_nome LIKE '06.05.03%' OR categoria_nome LIKE '06.10.01%'`
      );
```

ATENÇÃO: a query antiga de impostos tinha a MESMA forma (DESPESA+QUITADO+data_quitacao) com ordem de predicados diferente — o resultado é idêntico. NÃO mexa nas queries de inadimplência (4b, usa vencimento/nao_pago), outras receitas (4, competência), MRR (2) e pontual (3).

- [ ] **Step 3: Builder de linhas.** Adicionar tipo e função antes de `registerBp2026Routes`:

```typescript
interface DefLinha {
  metrica: string;
  titulo: string;
  tipoAgregacao: TipoAgregacao;
  direcao: Direcao;
  nota?: string;
}

function buildLinhas(
  defs: DefLinha[],
  orcado: Record<string, Record<number, number>>,
  realizadoPorMetrica: Record<string, (mes: number) => number | null>
): LinhaReceita[] {
  return defs.map(({ metrica, titulo, tipoAgregacao, direcao, nota }) => ({
    metrica,
    titulo,
    tipoAgregacao,
    direcao,
    nota,
    meses: Array.from({ length: 12 }, (_, i) => {
      const mes = i + 1;
      const o = orcado[metrica]?.[mes] ?? 0;
      const r = realizadoPorMetrica[metrica](mes);
      return { mes, orcado: o, realizado: r, atingimento: calcAtingimento(o, r) };
    }),
  }));
}
```

Tipar `LINHAS_DEDUCOES` e `LINHAS_CSV` como `DefLinha[]` (remover os tipos inline duplicados) e substituir os dois maps inline (blocos 6b e 6d) por:

```typescript
      // 6b. Deduções: inadimplência e impostos
      const deducoes = buildLinhas(LINHAS_DEDUCOES, orcado, realizadoPorMetrica);
      linhas.push(...deducoes);
```

```typescript
      // 6d. CSV: salários, benefício (rateado) e stack
      const linhasCsv = buildLinhas(LINHAS_CSV, orcado, realizadoPorMetrica);
      linhas.push(...linhasCsv);
```

O map das receitas (`LINHAS`, com `fonteAproximada`) NÃO muda.

- [ ] **Step 4: Verificar zero mudança de comportamento**

```bash
npx tsc --noEmit -p . 2>&1 | grep -i bp2026   # vazio
npx tsx ./bp2026-smoke.ts /tmp/bp2026-depois.json
diff /tmp/bp2026-antes.json /tmp/bp2026-depois.json && echo "IDÊNTICO"
```

Expected: `IDÊNTICO` (nenhuma diferença). Se os dados ao vivo mudarem entre as capturas (improvável no intervalo, cache do route é por processo), re-capture os dois em sequência.
Nota: o payload "antes" tem `nota: undefined` omitido no JSON para deduções; `buildLinhas` espalha `nota` como `undefined` para defs sem nota — JSON.stringify omite `undefined`, então o JSON permanece idêntico.

- [ ] **Step 5: Commit**

```bash
git add server/routes/bp2026.ts
git commit -m "refactor(bp2026): helper de despesa-caixa e builder de linhas (sem mudança de comportamento)"
```

---

### Task 3: API — CAC, SG&A, Bônus e EBITDA

**Files:**
- Modify: `server/routes/bp2026.ts`

- [ ] **Step 1: Definições.** Após `LINHAS_CSV`, adicionar:

```typescript
const NOTA_SGA =
  "Inclui o complemento do benefício (Caju) não atribuído ao CSV, " +
  "rateado pela fração orçada do mês.";

const NOTA_BONUS =
  "Realizado usa a categoria Premiações (05.01.10), que também inclui " +
  "premiações mensais orçadas no SG&A (~R$ 5k/mês).";

const LINHAS_OPEX: DefLinha[] = [
  { metrica: "cac", titulo: "(−) CAC", tipoAgregacao: "fluxo", direcao: "menor_melhor" },
  { metrica: "sga", titulo: "(−) SG&A", tipoAgregacao: "fluxo", direcao: "menor_melhor", nota: NOTA_SGA },
  { metrica: "bonus", titulo: "(−) Bônus", tipoAgregacao: "fluxo", direcao: "menor_melhor", nota: NOTA_BONUS },
];
```

- [ ] **Step 2: Queries** — após o bloco 4f, adicionar:

```typescript
      // 4g. CAC: caixa — comercial, mídia, eventos, brindes, viagens, locomoção
      const cacPorMes = await somaDespesaCaixaPorMes(
        db,
        sql`categoria_nome LIKE '05.04.02%' OR categoria_nome LIKE '06.04%'
            OR categoria_nome LIKE '06.05.04%' OR categoria_nome LIKE '06.05.05%'
            OR categoria_nome LIKE '06.06%' OR categoria_nome LIKE '06.07%'`
      );

      // 4h. SG&A (sem benefício): caixa — ocupação, tarifas, backoffice, pró-labore, adm
      const sgaBucketPorMes = await somaDespesaCaixaPorMes(
        db,
        sql`categoria_nome LIKE '06.02%' OR categoria_nome LIKE '06.03%'
            OR categoria_nome LIKE '06.08%' OR categoria_nome LIKE '06.09%'
            OR categoria_nome LIKE '06.10.02%' OR categoria_nome LIKE '06.10.03%'
            OR categoria_nome LIKE '06.10.06%' OR categoria_nome LIKE '06.10.07%'
            OR categoria_nome LIKE '06.10.08%'`
      );

      // 4i. Bônus: caixa — Premiações (05.01.10), excluída do CSV-Salários
      const bonusPorMes = await somaDespesaCaixaPorMes(db, sql`categoria_nome LIKE '05.01.10%'`);
```

- [ ] **Step 3: realizadoPorMetrica** — acrescentar:

```typescript
        cac: (mes) => (mes <= mesCorrente ? cacPorMes[mes] ?? 0 : null),
        sga: (mes) => {
          if (mes > mesCorrente) return null;
          // complemento do Caju: parcela não atribuída ao CSV (fecha o rateio da Parte 3)
          const complemento = ratear(
            beneficioTotalPorMes[mes] ?? 0,
            (orcado["beneficio_total_empresa"]?.[mes] ?? 0) - (orcado["csv_beneficio"]?.[mes] ?? 0),
            orcado["beneficio_total_empresa"]?.[mes] ?? 0
          );
          if (complemento === null) return null; // denominador inválido => seed quebrado, sinalizar
          return (sgaBucketPorMes[mes] ?? 0) + complemento;
        },
        bonus: (mes) => (mes <= mesCorrente ? bonusPorMes[mes] ?? 0 : null),
```

- [ ] **Step 4: Montagem** — após o push da `margem_bruta` (bloco 6e, que define `margemMeses`), adicionar:

```typescript
      // 6f. Opex: CAC, SG&A (com complemento do benefício) e Bônus
      const linhasOpex = buildLinhas(LINHAS_OPEX, orcado, realizadoPorMetrica);
      linhas.push(...linhasOpex);

      // 6g. EBITDA = Margem Bruta − CAC − SG&A − Bônus
      const ebitdaMeses = subtrairMeses(
        margemMeses,
        linhasOpex.map((l) => l.meses)
      );
      linhas.push({
        metrica: "ebitda",
        titulo: "(=) EBITDA",
        tipoAgregacao: "fluxo",
        direcao: "maior_melhor",
        meses: ebitdaMeses.map((m) => ({
          ...m,
          atingimento: calcAtingimento(m.orcado, m.realizado),
        })),
      });
```

- [ ] **Step 5: Smoke** — rodar `npx tsx ./bp2026-smoke.ts /tmp/bp2026-p4.json` e inspecionar `/tmp/bp2026-p4.json`:
- 15 linhas na ordem: …margem_bruta, cac, sga, bonus, ebitda
- cac jan: orcado 262495.7, realizado ≈238227 (90,8%)
- sga jan: orcado 227706, realizado ≈206196 (=189823 + 48678×14800/44000≈16373)
- bonus jan: orcado 54000, realizado ≈51664; bonus abr: orcado 0, realizado ≈3300, atingimento null
- ebitda jan: orcado ≈238731.27, realizado = margem jan − cac jan − sga jan − bonus jan (recompute)
- jul: realizado null nas 4; ytd coerente
Depois: `npx tsc --noEmit -p . 2>&1 | grep -i bp2026` vazio; `npx vitest run server/routes/bp2026.helpers.test.ts` 11/11. DELETAR `bp2026-smoke.ts`.

- [ ] **Step 6: Commit**

```bash
git add server/routes/bp2026.ts
git commit -m "feat(bp2026): linhas de CAC, SG&A (complemento do Caju), bônus e EBITDA"
```

---

### Task 4: Frontend — destaque do EBITDA

**Files:**
- Modify: `client/src/components/bp2026/BPDreTable.tsx`

- [ ] **Step 1:** Incluir `ebitda` no `ehTotal`:

```typescript
            const ehTotal =
              linha.metrica === "receita_total_faturavel" ||
              linha.metrica === "receita_liquida" ||
              linha.metrica === "margem_bruta" ||
              linha.metrica === "ebitda";
```

- [ ] **Step 2:** `npx tsc --noEmit -p . 2>&1 | grep -iE "bp2026|BP2026"` vazio; reiniciar dev server (`lsof -ti:3000 | xargs kill -9; npm run dev > /tmp/bp2026-dev-p4.log 2>&1 &`), `curl -s -o /dev/null -w "%{http_code}" localhost:3000/bp-2026` → 200. Validação visual do controller na sequência.

- [ ] **Step 3: Commit**

```bash
git add client/src/components/bp2026/BPDreTable.tsx
git commit -m "feat(bp2026): destaque da linha EBITDA"
```

---

### Task 5: Verificação final

- [ ] **Step 1:** `npx vitest run` — bp2026 11/11; ignorar 2 suites pré-existentes com falha de ambiente.
- [ ] **Step 2:** `git log --oneline origin/feature/bp2026-orcado-realizado..HEAD` — revisar.
- [ ] **Step 3:** Push; atualizar PR #247 via REST (`gh api repos/Warleypablo/Cortex/pulls/247 -X PATCH -F body=@/tmp/pr247-body.md` — o `gh pr edit` GraphQL está com 401).
