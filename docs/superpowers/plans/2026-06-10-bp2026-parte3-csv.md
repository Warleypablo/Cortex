# BP 2026 Parte 3 (CSV e Margem Bruta) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar (−) CSV — Salários, (−) CSV — Benefício (rateado), (−) CSV — Stack Tecnologia e (=) Margem Bruta à matriz de `/bp-2026`.

**Architecture:** Mesma extensão incremental das Partes 1–2: seed generaliza para múltiplas abas e ganha 4 métricas; endpoint ganha 3 queries caixa + helper `ratear` (rateio do benefício pela fração orçada) + linha derivada; frontend ganha campo `nota` (tooltip) e destaque da Margem Bruta.

**Tech Stack:** igual (Express + Drizzle sql tag, React + Tailwind, vitest).

**Spec:** `docs/superpowers/specs/2026-06-10-bp2026-parte3-csv-design.md`

**Contexto:** worktree `/Users/mac0267/Cortex/.claude/worktrees/bp2026-orcado-realizado`, branch `feature/bp2026-orcado-realizado` (PR #247). Xlsx só em `/Users/mac0267/Cortex/`. Senha prod via `.env` (`PROD_PASS=$(grep -E "^# *DB_PASSWORD=" .env | head -1 | sed -E 's/^# *DB_PASSWORD=//' | tr -d '"'"'"' \r')`). Referências (prod, caixa, 2026-06-10): salários jan=333.550, fev=366.693, mar=377.582, abr=383.816, mai=363.558; benefício total jan=48.678, fev=71.344, mar=54.339, abr=49.923, mai=50.935; stack jan=55.573, fev=72.291, mar=39.102, abr=30.839, mai=34.751. Orçado: csv_salarios jan=345.754,4; csv_beneficio jan=29.200; csv_stack jan=41.540; beneficio_total_empresa jan=44.000; margem bruta orçada jan=782.933 (derivada — confere com a planilha).

---

### Task 1: Seed multi-aba com 4 métricas novas

**Files:**
- Modify: `scripts/seed-bp2026-orcado.py` (reescrever o miolo para múltiplas abas)

- [ ] **Step 1: Reescrever o script** com este conteúdo completo:

```python
#!/usr/bin/env python3
"""Seed de cortex_core.bp2026_orcado a partir da planilha BP 2026.

Lê linhas de orçamento de várias abas (colunas C..N = Janeiro..Dezembro)
e gera /tmp/seed-bp2026-orcado.sql. Aborta se os totais lidos divergirem
dos totais conhecidos da planilha.
"""
import openpyxl

XLSX = "BP 2026 - Turbo - Financials.xlsx"
# (aba, linha, metrica)
LINHAS = [
    ("Overview", 4, "mrr_ativo"),
    ("Overview", 5, "receita_pontual"),
    ("Overview", 6, "outras_receitas"),
    ("Overview", 8, "inadimplencia"),
    ("Overview", 9, "impostos_receita"),
    ("Overview", 11, "csv_salarios"),
    ("Overview", 12, "csv_beneficio"),
    ("Overview", 13, "csv_stack"),
    ("SG&A", 11, "beneficio_total_empresa"),  # denominador do rateio do benefício
]
# Totais conhecidos da planilha, para verificação anti-drift
TOTAIS_ESPERADOS = {
    "mrr_ativo": 20998078.1,
    "receita_pontual": 4045000.0,
    "outras_receitas": 1040111.3,
    "inadimplencia": 1564991.4,
    "impostos_receita": 2422411.4,
    "csv_salarios": 6314099.1,
    "csv_beneficio": 481200.0,
    "csv_stack": 565344.0,
    "beneficio_total_empresa": 736000.0,
}

wb = openpyxl.load_workbook(XLSX, data_only=True)

stmts = []
for aba, row, metrica in LINHAS:
    ws = wb[aba]
    valores = [ws.cell(row=row, column=col).value for col in range(3, 15)]  # C..N
    assert all(isinstance(v, (int, float)) for v in valores), f"{metrica}: célula vazia/não numérica: {valores}"
    total = sum(valores)
    esperado = TOTAIS_ESPERADOS[metrica]
    assert abs(total - esperado) < 1, f"{metrica}: total lido {total:.1f} != esperado {esperado:.1f}"
    stmts.append(f"DELETE FROM cortex_core.bp2026_orcado WHERE metrica = '{metrica}';")
    for mes, valor in enumerate(valores, 1):
        stmts.append(
            f"INSERT INTO cortex_core.bp2026_orcado (metrica, mes, valor) VALUES ('{metrica}', {mes}, {round(valor, 2)});"
        )

with open("/tmp/seed-bp2026-orcado.sql", "w") as f:
    f.write("BEGIN;\n" + "\n".join(stmts) + "\nCOMMIT;\n")

print(f"OK: {len(stmts)} statements em /tmp/seed-bp2026-orcado.sql")
```

- [ ] **Step 2: Rodar e aplicar nos dois bancos**

```bash
cd /Users/mac0267/Cortex && python3 /Users/mac0267/Cortex/.claude/worktrees/bp2026-orcado-realizado/scripts/seed-bp2026-orcado.py
PGPASSWORD=dev123 psql -h localhost -U cortex -d cortex_dev -f /tmp/seed-bp2026-orcado.sql
PROD_PASS=$(grep -E "^# *DB_PASSWORD=" .env | head -1 | sed -E 's/^# *DB_PASSWORD=//' | tr -d '"'"'"' \r') && PGPASSWORD="$PROD_PASS" psql -h 34.95.249.110 -U postgres -d dados_turbo -f /tmp/seed-bp2026-orcado.sql
```

Expected: `OK: 117 statements` (9 DELETEs + 108 INSERTs), `COMMIT` nos dois.

- [ ] **Step 3: Verificar**

```bash
PGPASSWORD=dev123 psql -h localhost -U cortex -d cortex_dev -c "SELECT metrica, COUNT(*), ROUND(SUM(valor)) FROM cortex_core.bp2026_orcado GROUP BY metrica ORDER BY 1;"
```

Expected (9 linhas): beneficio_total_empresa 12 736000 | csv_beneficio 12 481200 | csv_salarios 12 6314099 | csv_stack 12 565344 | impostos_receita 12 2422411 | inadimplencia 12 1564991 | mrr_ativo 12 20998078 | outras_receitas 12 1040111 | receita_pontual 12 4045000. Mesma query/resultado em prod. Spot-check jan: `SELECT metrica, valor FROM cortex_core.bp2026_orcado WHERE mes=1 AND metrica LIKE 'csv%' OR mes=1 AND metrica='beneficio_total_empresa' ORDER BY 1;` → beneficio_total_empresa 44000, csv_beneficio 29200, csv_salarios ~345754.4, csv_stack 41540.

- [ ] **Step 4: Commit**

```bash
git add scripts/seed-bp2026-orcado.py
git commit -m "feat(bp2026): seed multi-aba — CSV salários/benefício/stack + benefício total"
```

---

### Task 2: Helper ratear (TDD)

**Files:**
- Modify: `server/routes/bp2026.helpers.ts`
- Test: `server/routes/bp2026.helpers.test.ts`

- [ ] **Step 1: Testes (falhando)** — acrescentar ao final do test file (e incluir `ratear` no import do topo):

```typescript
describe("ratear", () => {
  it("aplica a fração numerador/denominador ao valor", () => {
    expect(ratear(48678, 29200, 44000)).toBeCloseTo(32304.96, 1);
  });
  it("retorna null para valor null", () => {
    expect(ratear(null, 29200, 44000)).toBeNull();
  });
  it("retorna null com denominador zero (não mascara seed quebrado)", () => {
    expect(ratear(48678, 29200, 0)).toBeNull();
  });
});
```

- [ ] **Step 2:** `npx vitest run server/routes/bp2026.helpers.test.ts` → FAIL (`ratear is not a function`).

- [ ] **Step 3: Implementar** — ao final de `bp2026.helpers.ts`:

```typescript
// Rateio proporcional null-safe (usado para dividir o benefício entre CSV e SG&A
// pela fração orçada do mês). Denominador inválido => null, para expor erro de seed.
export function ratear(
  valor: number | null,
  numerador: number,
  denominador: number
): number | null {
  if (valor === null || !denominador) return null;
  return (valor * numerador) / denominador;
}
```

- [ ] **Step 4:** `npx vitest run server/routes/bp2026.helpers.test.ts` → PASS (11 testes: 8 + 3).

- [ ] **Step 5: Commit**

```bash
git add server/routes/bp2026.helpers.ts server/routes/bp2026.helpers.test.ts
git commit -m "feat(bp2026): helper ratear null-safe para o rateio do benefício"
```

---

### Task 3: API — linhas de CSV e Margem Bruta

**Files:**
- Modify: `server/routes/bp2026.ts`

Leia o arquivo inteiro antes (~280 linhas). Estado atual relevante: `LINHAS` (3 receitas), `LINHAS_DEDUCOES` (2), montagem em blocos numerados (1 orçado, 2 MRR, 3 pontual, 4 outras, 4b inadimplência, 4c impostos, 5 montagem, 6 total, 6b deduções, 6c receita líquida via `liquidaMeses`, 7 YTD).

- [ ] **Step 1: Import e definições.** Incluir `ratear` no import dos helpers. Na interface `LinhaReceita`, adicionar campo opcional após `direcao`:

```typescript
  nota?: string;
```

Após `LINHAS_DEDUCOES`, adicionar:

```typescript
const NOTA_BENEFICIO =
  "O benefício (Caju) não separa operação de administrativo no Conta Azul; " +
  "o realizado é rateado pela fração orçada do mês.";

const LINHAS_CSV: Array<{
  metrica: string;
  titulo: string;
  tipoAgregacao: TipoAgregacao;
  direcao: Direcao;
  nota?: string;
}> = [
  { metrica: "csv_salarios", titulo: "(−) CSV — Salários", tipoAgregacao: "fluxo", direcao: "menor_melhor" },
  { metrica: "csv_beneficio", titulo: "(−) CSV — Benefício", tipoAgregacao: "fluxo", direcao: "menor_melhor", nota: NOTA_BENEFICIO },
  { metrica: "csv_stack", titulo: "(−) CSV — Stack Tecnologia", tipoAgregacao: "fluxo", direcao: "menor_melhor" },
];
```

- [ ] **Step 2: Queries novas** — após o bloco `// 4c. Impostos sobre receita` (e seu loop), inserir:

```typescript
      // 4d. CSV Salários: caixa — folha de operação (05.01 exceto premiações) + freelancers (05.02)
      const salariosResult = await db.execute(sql`
        SELECT EXTRACT(MONTH FROM data_quitacao)::int AS mes,
               SUM(COALESCE(valor_pago::numeric, 0)) AS total
        FROM "Conta Azul".caz_parcelas
        WHERE tipo_evento = 'DESPESA'
          AND status = 'QUITADO'
          AND data_quitacao >= '2026-01-01' AND data_quitacao < '2027-01-01'
          AND ((categoria_nome LIKE '05.01%' AND categoria_nome NOT LIKE '05.01.10%')
               OR categoria_nome LIKE '05.02%')
        GROUP BY 1 ORDER BY 1
      `);
      const salariosPorMes: Record<number, number> = {};
      for (const row of salariosResult.rows as any[]) {
        salariosPorMes[Number(row.mes)] = parseFloat(row.total);
      }

      // 4e. Benefícios totais da empresa (Caju): caixa — rateado depois pela fração orçada
      const beneficioResult = await db.execute(sql`
        SELECT EXTRACT(MONTH FROM data_quitacao)::int AS mes,
               SUM(COALESCE(valor_pago::numeric, 0)) AS total
        FROM "Conta Azul".caz_parcelas
        WHERE tipo_evento = 'DESPESA'
          AND status = 'QUITADO'
          AND data_quitacao >= '2026-01-01' AND data_quitacao < '2027-01-01'
          AND categoria_nome LIKE '06.10.04%'
        GROUP BY 1 ORDER BY 1
      `);
      const beneficioTotalPorMes: Record<number, number> = {};
      for (const row of beneficioResult.rows as any[]) {
        beneficioTotalPorMes[Number(row.mes)] = parseFloat(row.total);
      }

      // 4f. CSV Stack: caixa — todo o software da empresa (conceito da planilha)
      const stackResult = await db.execute(sql`
        SELECT EXTRACT(MONTH FROM data_quitacao)::int AS mes,
               SUM(COALESCE(valor_pago::numeric, 0)) AS total
        FROM "Conta Azul".caz_parcelas
        WHERE tipo_evento = 'DESPESA'
          AND status = 'QUITADO'
          AND data_quitacao >= '2026-01-01' AND data_quitacao < '2027-01-01'
          AND (categoria_nome LIKE '05.03%' OR categoria_nome LIKE '05.04.01%'
               OR categoria_nome LIKE '06.05.03%' OR categoria_nome LIKE '06.10.01%')
        GROUP BY 1 ORDER BY 1
      `);
      const stackPorMes: Record<number, number> = {};
      for (const row of stackResult.rows as any[]) {
        stackPorMes[Number(row.mes)] = parseFloat(row.total);
      }
```

- [ ] **Step 3: realizadoPorMetrica** — acrescentar (depois das entradas de deduções; `orcado` já está populado nesse ponto):

```typescript
        csv_salarios: (mes) => (mes <= mesCorrente ? salariosPorMes[mes] ?? 0 : null),
        csv_beneficio: (mes) =>
          mes <= mesCorrente
            ? ratear(
                beneficioTotalPorMes[mes] ?? 0,
                orcado["csv_beneficio"]?.[mes] ?? 0,
                orcado["beneficio_total_empresa"]?.[mes] ?? 0
              )
            : null,
        csv_stack: (mes) => (mes <= mesCorrente ? stackPorMes[mes] ?? 0 : null),
```

- [ ] **Step 4: Montagem** — após o push da `receita_liquida` (bloco 6c, que define `liquidaMeses`), acrescentar:

```typescript
      // 6d. CSV: salários, benefício (rateado) e stack
      const linhasCsv: LinhaReceita[] = LINHAS_CSV.map(
        ({ metrica, titulo, tipoAgregacao, direcao, nota }) => ({
          metrica,
          titulo,
          tipoAgregacao,
          direcao,
          nota,
          meses: Array.from({ length: 12 }, (_, i) => {
            const mes = i + 1;
            const o = orcado[metrica]?.[mes] ?? 0;
            const r = realizadoPorMetrica[metrica](mes);
            return { mes, orcado: o, realizado: r, atingimento: calcAtingimento(o, r) };
          }),
        })
      );
      linhas.push(...linhasCsv);

      // 6e. Margem Bruta = Receita Líquida − CSV (salários + benefício + stack)
      const margemMeses = subtrairMeses(
        liquidaMeses,
        linhasCsv.map((l) => l.meses)
      );
      linhas.push({
        metrica: "margem_bruta",
        titulo: "(=) Margem Bruta",
        tipoAgregacao: "fluxo",
        direcao: "maior_melhor",
        meses: margemMeses.map((m) => ({
          ...m,
          atingimento: calcAtingimento(m.orcado, m.realizado),
        })),
      });
```

IMPORTANTE: a métrica `beneficio_total_empresa` existe no map `orcado` mas NÃO vira linha do payload.

- [ ] **Step 5: Smoke test** — criar `bp2026-smoke.ts` na raiz (rodar e DELETAR antes do commit):

```typescript
import "dotenv/config";
import express from "express";
import { registerBp2026Routes } from "./server/routes/bp2026";
import { db } from "./server/db";
const app = express();
registerBp2026Routes(app, db);
const server = app.listen(3994, async () => {
  const res = await fetch("http://localhost:3994/api/bp2026/receitas");
  const json: any = await res.json();
  console.log(json.linhas.map((l: any) => l.metrica).join(","));
  for (const m of ["csv_salarios", "csv_beneficio", "csv_stack", "margem_bruta"]) {
    const l = json.linhas.find((x: any) => x.metrica === m);
    const jan = l.meses[0];
    console.log(m, l.direcao, l.nota ? "[nota]" : "", "jan:", jan.orcado, jan.realizado, "jul:", l.meses[6].realizado, "ytd:", JSON.stringify(l.ytd));
  }
  server.close(); process.exit(0);
});
```

Run: `npx tsx ./bp2026-smoke.ts`
Expected:
- 11 metricas na ordem: mrr_ativo, receita_pontual, outras_receitas, receita_total_faturavel, inadimplencia, impostos_receita, receita_liquida, csv_salarios, csv_beneficio, csv_stack, margem_bruta (sem beneficio_total_empresa)
- csv_salarios jan: orcado 345754.4, realizado ≈333550
- csv_beneficio jan: orcado 29200, realizado ≈32305 (=48678×29200/44000), com `[nota]`
- csv_stack jan: orcado 41540, realizado ≈55573
- margem_bruta jan: orcado ≈782932.97 (planilha: 782.933 ✓), realizado = receita_liquida jan − soma dos 3 CSVs jan
- jul: realizado null nas 4 linhas; ytd das 4 = acumulado jan..mai

Also: `npx tsc --noEmit -p . 2>&1 | grep -i bp2026` → vazio; `npx vitest run server/routes/bp2026.helpers.test.ts` → 11/11.

- [ ] **Step 6: Commit**

```bash
git add server/routes/bp2026.ts
git commit -m "feat(bp2026): linhas de CSV (salários, benefício rateado, stack) e margem bruta"
```

---

### Task 4: Frontend — nota em tooltip e destaque da Margem Bruta

**Files:**
- Modify: `client/src/components/bp2026/BPDreTable.tsx`

- [ ] **Step 1: Interface** — em `BPLinha`, adicionar após `direcao`:

```typescript
  nota?: string;
```

- [ ] **Step 2: Tooltip da nota** — no `<td>` do título (onde já existe o tooltip de `ehEstoque`), acrescentar logo após o bloco `{ehEstoque && (...)}`:

```tsx
                    {linha.nota && (
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Info className="h-3 w-3 text-gray-400 dark:text-zinc-500" />
                        </TooltipTrigger>
                        <TooltipContent className="max-w-72">{linha.nota}</TooltipContent>
                      </Tooltip>
                    )}
```

- [ ] **Step 3: Destaque** — incluir `margem_bruta` no `ehTotal`:

```typescript
            const ehTotal =
              linha.metrica === "receita_total_faturavel" ||
              linha.metrica === "receita_liquida" ||
              linha.metrica === "margem_bruta";
```

- [ ] **Step 4: Verificar**

```bash
npx tsc --noEmit -p . 2>&1 | grep -iE "bp2026|BP2026"   # vazio
cd /Users/mac0267/Cortex/.claude/worktrees/bp2026-orcado-realizado
lsof -ti:3000 | xargs kill -9
npm run dev > /tmp/bp2026-dev-p3.log 2>&1 &
sleep 10
curl -s -o /dev/null -w "%{http_code}" localhost:3000/bp-2026   # 200
```

Validação visual (11 linhas, tooltip da nota no Benefício, Margem Bruta destacada, dark+light) é do controller na sequência.

- [ ] **Step 5: Commit**

```bash
git add client/src/components/bp2026/BPDreTable.tsx
git commit -m "feat(bp2026): nota em tooltip por linha e destaque da margem bruta"
```

---

### Task 5: Verificação final

- [ ] **Step 1:** `npx vitest run` — 11 testes bp2026 verdes; 2 suites com falha de ambiente pré-existente (sdr-assistant, replyClassifier) — ignorar.
- [ ] **Step 2:** `git log --oneline origin/feature/bp2026-orcado-realizado..HEAD` — revisar commits.
- [ ] **Step 3:** Push; atualizar corpo do PR #247 com a seção Parte 3 via `gh api repos/Warleypablo/Cortex/pulls/247 -X PATCH -F body=@<arquivo>` (o `gh pr edit` está com 401 no GraphQL; usar REST).
