# BP 2026 — Ajustes na aba CAC — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar na aba CAC do BP 2026 (1) uma seção "CAC por Produto" (rateio do CAC pelos contratos vendidos de cada produto) e (2) uma linha-filha de "% do CAC total" sob cada item de custo.

**Architecture:** Toda a matemática vira helpers puros testáveis (`bp2026.cac.helpers.ts`). `montarDetalhamentos` consome esses helpers para montar as linhas novas; o handler `bp2026.ts` carrega a atribuição de vendas uma única vez e deriva os contratos recorrentes vendidos por produto. O frontend ganha dois flags de linha (`subItem`, `semDetalhe`) para indentar as linhas-filha e desabilitar drill.

**Tech Stack:** TypeScript, Express, Drizzle, vitest (test runner: `vitest run`), React + Tailwind.

## Global Constraints

- Test runner: `npx vitest run <arquivo>` (ou `npm test`). Testes ficam colocados como `*.test.ts` ao lado do código.
- Dark/light mode obrigatório no frontend (classes `dark:`).
- Commits: Conventional Commits, terminar com `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Branch: `feature/bp2026-cac-ajustes` (já criada).
- 5 produtos recorrentes (slug → segmento): `performance`→"Performance", `creators`→"Creators", `social`→"Social", `gc`→"Gestão de Comunidade", `others`→"Others".
- Rateio: `CAC_produto(m) = CAC_total(m) × contratos_produto(m) / Σ contratos(m)`. Soma das 5 linhas = CAC total. Denominador 0 ou total null → célula null.
- % do total: realizado% = sub-linha ÷ CAC total realizado; orçado% = sub-orçado ÷ CAC orçado total; YTD = Σ/Σ (não média de razões); atingimento sempre null.

---

### Task 1: Helpers puros de rateio e participação

**Files:**
- Create: `server/routes/bp2026.cac.helpers.ts`
- Test: `server/routes/bp2026.cac.helpers.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `ratearSeriePorPeso(total: (number|null)[], pesos: Record<string, (number|null)[]>): Record<string, (number|null)[]>`
  - `participacaoPct(parte: (number|null)[], total: (number|null)[]): (number|null)[]`
  - `razaoYtd(num: (number|null)[], den: (number|null)[], mesFechado: number): number | null`

- [ ] **Step 1: Escrever os testes que falham**

Criar `server/routes/bp2026.cac.helpers.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { ratearSeriePorPeso, participacaoPct, razaoYtd } from "./bp2026.cac.helpers";

describe("ratearSeriePorPeso", () => {
  it("rateia proporcional aos pesos e soma fecha com o total", () => {
    const r = ratearSeriePorPeso([300000, null], { a: [30, null], b: [10, null] });
    expect(r.a).toEqual([225000, null]);
    expect(r.b).toEqual([75000, null]);
    expect((r.a[0] as number) + (r.b[0] as number)).toBe(300000);
  });

  it("peso 0 num produto => alocação 0 (os demais absorvem o total)", () => {
    const r = ratearSeriePorPeso([100], { a: [0], b: [10] });
    expect(r.a).toEqual([0]);
    expect(r.b).toEqual([100]);
  });

  it("soma dos pesos = 0 no mês => null para todos", () => {
    const r = ratearSeriePorPeso([100], { a: [0], b: [0] });
    expect(r.a).toEqual([null]);
    expect(r.b).toEqual([null]);
  });

  it("total null no mês => null para todos", () => {
    const r = ratearSeriePorPeso([null], { a: [5], b: [5] });
    expect(r.a).toEqual([null]);
    expect(r.b).toEqual([null]);
  });
});

describe("participacaoPct", () => {
  it("razão parte/total por mês, null quando indefinido", () => {
    expect(participacaoPct([50, null, 10], [200, 100, null])).toEqual([0.25, null, null]);
  });
  it("denominador 0 => null", () => {
    expect(participacaoPct([5], [0])).toEqual([null]);
  });
});

describe("razaoYtd", () => {
  it("Σnum / Σden até mesFechado", () => {
    expect(razaoYtd([10, 20, 30], [100, 200, 300], 2)).toBeCloseTo(0.1, 6); // 30/300
  });
  it("ignora meses null no numerador e denominador", () => {
    expect(razaoYtd([10, null, 30], [100, null, 300], 3)).toBeCloseTo(0.1, 6); // 40/400
  });
  it("Σden = 0 => null", () => {
    expect(razaoYtd([0], [0], 1)).toBeNull();
  });
  it("mesFechado 0 => null", () => {
    expect(razaoYtd([10], [100], 0)).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar os testes e ver falhar**

Run: `npx vitest run server/routes/bp2026.cac.helpers.test.ts`
Expected: FAIL — `Failed to resolve import "./bp2026.cac.helpers"`.

- [ ] **Step 3: Implementar os helpers**

Criar `server/routes/bp2026.cac.helpers.ts`:

```ts
// server/routes/bp2026.cac.helpers.ts
// Helpers puros para a aba CAC do BP 2026: rateio do CAC por produto e
// participação percentual de cada item no total. Sem acesso a banco.

// Rateia uma série total entre chaves, proporcional aos pesos de cada chave por mês.
// total[i] === null OU Σ pesos do mês === 0  => alocação null naquele mês para todas as chaves.
export function ratearSeriePorPeso(
  total: (number | null)[],
  pesos: Record<string, (number | null)[]>,
): Record<string, (number | null)[]> {
  const chaves = Object.keys(pesos);
  const n = total.length;
  const out: Record<string, (number | null)[]> = {};
  for (const k of chaves) out[k] = [];
  for (let i = 0; i < n; i++) {
    const somaPesos = chaves.reduce((s, k) => s + (pesos[k]?.[i] ?? 0), 0);
    const t = total[i];
    for (const k of chaves) {
      if (t === null || somaPesos === 0) {
        out[k].push(null);
      } else {
        out[k].push((t * (pesos[k]?.[i] ?? 0)) / somaPesos);
      }
    }
  }
  return out;
}

// Participação de `parte` em `total`, mês a mês (null quando indefinido ou denominador 0).
export function participacaoPct(
  parte: (number | null)[],
  total: (number | null)[],
): (number | null)[] {
  return parte.map((p, i) => {
    const t = total[i];
    return p === null || t === null || !t ? null : p / t;
  });
}

// Razão de somas acumuladas até mesFechado: Σ num / Σ den (ignora posições null).
// Σ den === 0 ou mesFechado === 0 => null.
export function razaoYtd(
  num: (number | null)[],
  den: (number | null)[],
  mesFechado: number,
): number | null {
  if (mesFechado <= 0) return null;
  let somaNum = 0;
  let somaDen = 0;
  for (let i = 0; i < mesFechado; i++) {
    if (num[i] !== null) somaNum += num[i] as number;
    if (den[i] !== null) somaDen += den[i] as number;
  }
  return somaDen === 0 ? null : somaNum / somaDen;
}
```

- [ ] **Step 4: Rodar os testes e ver passar**

Run: `npx vitest run server/routes/bp2026.cac.helpers.test.ts`
Expected: PASS (todos os describes verdes).

- [ ] **Step 5: Commit**

```bash
git add server/routes/bp2026.cac.helpers.ts server/routes/bp2026.cac.helpers.test.ts
git commit -m "$(printf 'feat(bp2026): helpers de rateio e participacao do CAC\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

### Task 2: Backend — CAC por produto + linhas-filha de %

**Files:**
- Modify: `server/routes/bp2026.vendasProduto.ts` (Deps aceita `atrib?` injetado)
- Modify: `server/routes/bp2026.ts` (carrega atribuição 1×, deriva contratos por produto, passa às duas funções)
- Modify: `server/routes/bp2026.detalhamentos.ts` (interface `Linha` + construção das linhas novas)

**Interfaces:**
- Consumes (Task 1): `ratearSeriePorPeso`, `participacaoPct`, `razaoYtd`.
- Consumes (existente): `carregarAtribuicaoVendas(db): Promise<AtribuicaoVendas>` e `AtribuicaoVendas` de `./bp2026.vendasProduto`; `agregarVendasProduto(deals, prMix, mixRec, mixPont, aovRec, aovPont): Map<number, Map<SegmentoBP, CelulaSeg>>` de `./bp2026.vendasProduto.helpers`; `SegmentoBP` de `../okr2026/servicosBitrix`.
- Produces: payload `cacDetalhe` com novas linhas `cac_produto_<slug>` (campo `grupo: "CAC por Produto"`, `semDetalhe: true`) e `<metrica>_pct_total` (campos `subItem: true`, `semDetalhe: true`); nova dep `contratosVendidosRec` em `montarDetalhamentos`.

- [ ] **Step 1: `bp2026.vendasProduto.ts` — aceitar atribuição injetada**

Em `interface Deps` (≈ linha 38), adicionar o campo opcional:

```ts
interface Deps {
  db: any;
  orcado: Record<string, Record<number, number>>;
  mesCorrente: number;
  mesFechado: number;
  atrib?: AtribuicaoVendas; // reaproveita a atribuição já carregada pelo handler
}
```

Em `montarVendasProduto` (≈ linha 148), trocar a linha que carrega a atribuição:

```ts
  const { deals, prMix, mixRec, mixPont, aovRec, aovPont } = deps.atrib ?? await carregarAtribuicaoVendas(db);
```

- [ ] **Step 2: `bp2026.detalhamentos.ts` — estender interface `Linha` e a dep**

Na `interface Linha` (≈ linha 11), adicionar os 3 campos opcionais:

```ts
interface Linha {
  metrica: string; titulo: string; tipoAgregacao: "fluxo" | "estoque";
  direcao: "maior_melhor" | "menor_melhor" | "neutro";
  unidade?: "brl" | "int" | "pct" | "dec"; nota?: string; destaque?: boolean;
  grupo?: string; subItem?: boolean; semDetalhe?: boolean;
  meses: MesLinha[];
  ytd: { orcado: number; realizado: number | null; atingimento: number | null };
}
```

Na `interface Deps` (≈ linha 30), adicionar:

```ts
  // realizado de contratos recorrentes vendidos por produto (slug -> série 12 meses, null no futuro)
  contratosVendidosRec: Record<string, (number | null)[]>;
```

Adicionar o import no topo do arquivo (após a linha 8):

```ts
import { ratearSeriePorPeso, participacaoPct, razaoYtd } from "./bp2026.cac.helpers";
```

E uma constante de produtos + nota (após `NOTA_CAC_OUTRAS`, ≈ linha 54):

```ts
const NOTA_CAC_PRODUTO =
  "CAC total do mês rateado pela participação do produto nos contratos recorrentes " +
  "vendidos (Bitrix). Soma dos produtos = CAC total. Pontual não recebe bucket próprio.";

const PRODUTOS_CAC: { slug: string; titulo: string }[] = [
  { slug: "performance", titulo: "Performance" },
  { slug: "creators", titulo: "Creators" },
  { slug: "social", titulo: "Social" },
  { slug: "gc", titulo: "Gestão de Comunidade" },
  { slug: "others", titulo: "Others" },
];
```

- [ ] **Step 3: `bp2026.detalhamentos.ts` — montar linhas-filha de % e bloco por produto**

Atualizar a desestruturação de `deps` (≈ linha 81) para incluir a nova dep:

```ts
  const { db, orcado, vendasMrrPorMes, pontualPorMes, ganhosPorMes, contratosVendidosRec, mesCorrente, mesFechado } = deps;
```

Inserir **logo antes do `return`** (≈ linha 211, após a definição de `cacPayback` e do `outrasResult`/`orTotal`; assim `razao`, `cacSeries`, `cacLinhas`, `cacTotalSerie` e `cacOrcMes` já estão todos definidos). Montar as linhas novas:

```ts
  // ---- linha-filha "% do CAC total" sob cada sub-linha de custo ----
  const cacOrcSerie = Array.from({ length: 12 }, (_, i) => cacOrcMes(i + 1));
  const subOrcSerie = (def: typeof SUB_CAC[number]) =>
    Array.from({ length: 12 }, (_, i) => (def.semOrcado ? 0 : orcado[def.metrica]?.[i + 1] ?? 0));

  const cacLinhasComPct: Linha[] = [];
  SUB_CAC.forEach((def, k) => {
    cacLinhasComPct.push(cacLinhas[k]);
    const pctReal = participacaoPct(cacSeries[k], cacTotalSerie);
    const orcDoMes = subOrcSerie(def);
    const pctChild: Linha = {
      ...fazLinha(
        { metrica: `${def.metrica}_pct_total`, titulo: "↳ % do CAC total", direcao: "neutro", unidade: "pct" },
        pctReal,
        (m) => razao(orcDoMes[m - 1], cacOrcSerie[m - 1]) ?? 0,
        mesFechado === 0 ? undefined : {
          orcado: razaoYtd(orcDoMes, cacOrcSerie, mesFechado) ?? 0,
          realizado: razaoYtd(cacSeries[k], cacTotalSerie, mesFechado),
        },
      ),
      subItem: true,
      semDetalhe: true,
    };
    // linha de composição: sem semântica de meta
    pctChild.meses = pctChild.meses.map((m) => ({ ...m, atingimento: null }));
    pctChild.ytd = { ...pctChild.ytd, atingimento: null };
    cacLinhasComPct.push(pctChild);
  });

  // ---- bloco "CAC por Produto": rateio do CAC pelos contratos vendidos ----
  const orcContratos: Record<string, (number | null)[]> = {};
  for (const p of PRODUTOS_CAC) {
    orcContratos[p.slug] = Array.from({ length: 12 }, (_, i) => orcado[`contratos_vendidos_mrr_${p.slug}`]?.[i + 1] ?? 0);
  }
  const allocReal = ratearSeriePorPeso(cacTotalSerie, contratosVendidosRec);
  const allocOrc = ratearSeriePorPeso(cacOrcSerie, orcContratos);
  const cacPorProduto: Linha[] = PRODUTOS_CAC.map((p) => ({
    ...fazLinha(
      { metrica: `cac_produto_${p.slug}`, titulo: p.titulo, direcao: "menor_melhor", nota: NOTA_CAC_PRODUTO },
      allocReal[p.slug],
      (m) => allocOrc[p.slug][m - 1] ?? 0,
    ),
    grupo: "CAC por Produto",
    semDetalhe: true,
  }));
```

Trocar o `return` da função (≈ linha 244) para usar `cacLinhasComPct` e anexar o bloco por produto:

```ts
  return {
    sga: [sgaTotal, ...sgaLinhas],
    cac: [cacTotal, ...cacLinhasComPct, cacPorCliente, cacPctReceita, cacPayback, ...cacPorProduto],
    outrasReceitas: [orTotal, variavelL, stackL, demaisL],
  };
```

- [ ] **Step 4: `bp2026.ts` — carregar atribuição 1×, derivar contratos por produto, ligar às funções**

Adicionar imports no topo (junto dos demais imports de bp2026.vendasProduto):

```ts
import { montarVendasProduto, carregarAtribuicaoVendas } from "./bp2026.vendasProduto";
import { agregarVendasProduto } from "./bp2026.vendasProduto.helpers";
import type { SegmentoBP } from "../okr2026/servicosBitrix";
```
(Se já existir `import { montarVendasProduto } from "./bp2026.vendasProduto";`, substituir por esta linha combinada.)

Substituir o trecho que chama `montarVendasProduto` (≈ linha 517) por:

```ts
      // 12b. Atribuição de vendas: carregada uma vez e reaproveitada (vendas por produto + rateio do CAC)
      const atrib = await carregarAtribuicaoVendas(db);
      const agg = agregarVendasProduto(atrib.deals, atrib.prMix, atrib.mixRec, atrib.mixPont, atrib.aovRec, atrib.aovPont);
      const SEG_CAC: [string, SegmentoBP][] = [
        ["performance", "Performance"], ["creators", "Creators"], ["social", "Social"],
        ["gc", "Gestão de Comunidade"], ["others", "Others"],
      ];
      const contratosVendidosRec: Record<string, (number | null)[]> = {};
      for (const [slug, seg] of SEG_CAC) {
        contratosVendidosRec[slug] = Array.from({ length: 12 }, (_, i) =>
          i + 1 <= mesCorrente ? (agg.get(i + 1)?.get(seg)?.contratosRec ?? 0) : null);
      }
      const vendasProduto = await montarVendasProduto({ db, orcado, mesCorrente, mesFechado, atrib });
```

Atualizar a chamada de `montarDetalhamentos` (≈ linha 528) para passar a nova dep:

```ts
      const { sga: sgaDetalhe, cac: cacDetalhe, outrasReceitas: outrasDetalhe } = await montarDetalhamentos({
        db, orcado, vendasMrrPorMes, pontualPorMes, ganhosPorMes, contratosVendidosRec, mesCorrente, mesFechado,
      });
```

- [ ] **Step 5: Typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: sem erros novos referentes a `bp2026*` (se o projeto não tiver `tsc` configurado para o server, usar `npx vitest run server/routes/bp2026.cac.helpers.test.ts` + `npm run build` conforme `package.json`; o importante é que não haja erro de tipo nas novas linhas).

- [ ] **Step 6: Smoke do endpoint (dados)**

Reiniciar o dev server e verificar o payload:

```bash
lsof -ti:3000 | xargs kill -9 2>/dev/null; (npm run dev >/tmp/cortex-dev.log 2>&1 &) ; sleep 6
```

Como `/api` exige auth, validar via browser autenticado (Claude-in-Chrome) ou inspecionando `/tmp/cortex-dev.log` por erros. No browser, abrir `http://localhost:3000/bp-2026` → aba **CAC** e confirmar:
- bloco "CAC por Produto" com 5 linhas (Performance, Creators, Social, Gestão de Comunidade, Others);
- sob cada item de custo, uma linha "↳ % do CAC total";
- num mês fechado, soma das 5 linhas de produto ≈ valor da linha "CAC (soma das sub-linhas)".

Expected: as linhas aparecem; sem erro 500 no log.

- [ ] **Step 7: Commit**

```bash
git add server/routes/bp2026.ts server/routes/bp2026.detalhamentos.ts server/routes/bp2026.vendasProduto.ts
git commit -m "$(printf 'feat(bp2026): CAC por produto e %% do total por item na aba CAC\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

### Task 3: Frontend — render de linha-filha indentada e linhas sem drill

**Files:**
- Modify: `client/src/components/bp2026/BPDreTable.tsx` (interface `BPLinha` + render)

**Interfaces:**
- Consumes (Task 2): linhas com `subItem?: boolean` e `semDetalhe?: boolean` no payload `cacDetalhe`.
- Produces: render indentado/atenuado para `subItem`; células não-clicáveis quando `semDetalhe`.

- [ ] **Step 1: Adicionar os campos à interface `BPLinha`**

Em `client/src/components/bp2026/BPDreTable.tsx`, na `interface BPLinha` (≈ linha 15), adicionar após `segmento?: string;`:

```ts
  subItem?: boolean;    // linha-filha (ex.: "% do CAC total") — indentada e atenuada
  semDetalhe?: boolean; // célula não abre drill-down
```

- [ ] **Step 2: Desabilitar drill em linhas `semDetalhe`**

Na montagem das células de mês (≈ linha 225), trocar a definição de `clicavel`:

```ts
                    const clicavel = !!onCellClick && m.realizado !== null && !linha.subItem && !linha.semDetalhe;
```

- [ ] **Step 3: Estilo indentado/atenuado para `subItem`**

Na `<td>` do rótulo (sticky, ≈ linha 187-191), incluir o estilo de `subItem`. Substituir o `className` da `<td>` por:

```tsx
                <td
                  className={`sticky left-0 z-10 px-4 py-3 whitespace-nowrap align-top ${
                    ehTotal ? "bg-gray-100 dark:bg-zinc-800" : "bg-white dark:bg-zinc-900"
                  } ${linha.segmento ? "pl-8" : ""} ${linha.subItem ? "pl-8 text-xs text-gray-500 dark:text-zinc-500" : ""}`}
                >
```

(O `pl-8` repetido é inofensivo; `subItem` adiciona o tom atenuado e fonte menor.)

- [ ] **Step 4: Verificar no browser (dark + light)**

Com o dev server rodando, abrir `http://localhost:3000/bp-2026` → aba **CAC**:
- linhas "↳ % do CAC total" aparecem indentadas e em cinza, logo abaixo de cada item de custo;
- clicar numa linha "% do CAC total" ou numa linha de "CAC por Produto" NÃO abre o painel de drill;
- clicar numa sub-linha de custo (ex.: "ADs") CONTINUA abrindo o drill;
- conferir os dois temas (toggle de tema): contraste ok no claro e no escuro.

Expected: comportamento acima confirmado visualmente.

- [ ] **Step 5: Commit**

```bash
git add client/src/components/bp2026/BPDreTable.tsx
git commit -m "$(printf 'feat(bp2026): render de linha-filha indentada e linhas sem drill na aba CAC\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Self-Review (preenchido)

**Spec coverage:**
- CAC por produto (rateio por contratos, 5 recorrentes, R$) → Task 1 (helper `ratearSeriePorPeso`) + Task 2 (bloco `cac_produto_*`) + Task 3 (render).
- % do total por item (linha-filha) → Task 1 (`participacaoPct`/`razaoYtd`) + Task 2 (linhas `*_pct_total`) + Task 3 (indent/sem drill).
- Reaproveitar atribuição sem query duplicada → Task 2 Steps 1 e 4.
- 150k fora de escopo → nenhuma task (correto).

**Placeholder scan:** sem TBD/TODO; todo código está completo.

**Type consistency:** `ratearSeriePorPeso`/`participacaoPct`/`razaoYtd` (Task 1) usadas com as mesmas assinaturas em Task 2; `contratosVendidosRec: Record<string,(number|null)[]>` casado entre `bp2026.ts` (produz) e `bp2026.detalhamentos.ts` (consome); `subItem`/`semDetalhe` definidos em Task 2 (backend `Linha`) e Task 3 (frontend `BPLinha`). `razao` já existe em `montarDetalhamentos` (≈ linha 156) — reutilizada na linha-filha.

## Notas de execução

- O CAC orçado total = Σ das 9 sub-linhas orçadas (cac_outras_sub é `semOrcado`); `cacOrcMes(m)` já reflete isso.
- `mesFechado === 0`: as linhas novas seguem o padrão (YTD `{orcado:0, realizado:null}`), pois `fazLinha` e `razaoYtd` já tratam.
- A nota (`nota`) das linhas de produto sobrevive ao `anexarInfo` do handler (ele só sobrescreve `info`). Por isso usamos `nota`, não `info` — sem necessidade de alterar `anexarInfo`.
