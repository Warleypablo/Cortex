# Acesso por aba no BP 2026 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restringir, por pessoa, quais das 11 abas do "Orçado × Realizado" (BP 2026) cada usuário enxerga, com confidencialidade real (o dado de uma aba não permitida não trafega para o cliente).

**Architecture:** Whitelist por usuário numa coluna nova `auth_users.allowedBpTabs` (admin tem bypass). O backend filtra o payload de `/api/bp2026/receitas` (omite as chaves das abas não liberadas) e barra os endpoints de drill (`/detalhe`, `/reconciliacao`, `/pontual-creators`) via um catálogo métrica→aba. O front esconde as abas não liberadas e passa a aba de origem em cada drill. Lógica de "quais abas o usuário pode ver" e o catálogo são funções puras, testadas por unidade.

**Tech Stack:** TypeScript, Express, Drizzle ORM (PostgreSQL), React + React Query, Vitest, Tailwind.

## Global Constraints

- Dark/light mode obrigatório em qualquer UI nova (classes `dark:` do Tailwind).
- Toda alteração de schema é aplicada em **local E produção** (regra do projeto).
- Confidencialidade real: o reforço que protege o dado é o do **backend**; o front é só UX.
- Admin (`role === 'admin'`) enxerga todas as 11 abas (bypass da whitelist).
- Whitelist: usuário sem grant não vê nenhuma aba.
- Abas-resumo `dre` e `metricas` são tratadas como sensíveis (entram na whitelist normal).
- Co-author dos commits: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Lista canônica das 11 abas (id): `dre, metricas, revenue, funil, vendasProduto, capacity, sga, cac, outras, pontual, pontual-creators`.

---

### Task 1: Fonte única das abas + helper de permissão (compartilhado)

**Files:**
- Create: `shared/bp2026-tabs.ts`
- Test: `shared/bp2026-tabs.test.ts`

**Interfaces:**
- Produces:
  - `BP2026_TABS: ReadonlyArray<{ id: string; label: string }>`
  - `type Bp2026TabId` (union dos 11 ids)
  - `BP2026_TAB_IDS: Bp2026TabId[]`
  - `abasPermitidas(role: string | null | undefined, allowedBpTabs: string[] | null | undefined): Bp2026TabId[]` — admin → todas; senão filtra `allowedBpTabs` mantendo só ids válidos, preservando a ordem canônica.

- [ ] **Step 1: Write the failing test**

```ts
// shared/bp2026-tabs.test.ts
import { describe, it, expect } from "vitest";
import { BP2026_TABS, BP2026_TAB_IDS, abasPermitidas } from "./bp2026-tabs";

describe("BP2026_TABS", () => {
  it("tem as 11 abas na ordem canônica", () => {
    expect(BP2026_TAB_IDS).toEqual([
      "dre", "metricas", "revenue", "funil", "vendasProduto", "capacity",
      "sga", "cac", "outras", "pontual", "pontual-creators",
    ]);
  });
});

describe("abasPermitidas", () => {
  it("admin vê todas, ignorando allowedBpTabs", () => {
    expect(abasPermitidas("admin", [])).toEqual(BP2026_TAB_IDS);
    expect(abasPermitidas("admin", null)).toEqual(BP2026_TAB_IDS);
  });
  it("user sem grant não vê nada", () => {
    expect(abasPermitidas("user", [])).toEqual([]);
    expect(abasPermitidas("user", null)).toEqual([]);
  });
  it("user vê só as abas liberadas, na ordem canônica e sem ids inválidos", () => {
    expect(abasPermitidas("user", ["cac", "dre", "inexistente"])).toEqual(["dre", "cac"]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run shared/bp2026-tabs.test.ts`
Expected: FAIL — `Cannot find module './bp2026-tabs'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// shared/bp2026-tabs.ts
// Fonte única das abas do BP 2026 (Orçado × Realizado) e da regra de quais
// abas um usuário pode ver. Sem dependências de banco — usável no front e no back.

export const BP2026_TABS = [
  { id: "dre", label: "Overview" },
  { id: "metricas", label: "Métricas Gerais" },
  { id: "revenue", label: "Revenue" },
  { id: "funil", label: "Funil Comercial" },
  { id: "vendasProduto", label: "Vendas por Produto" },
  { id: "capacity", label: "Capacity" },
  { id: "sga", label: "SG&A" },
  { id: "cac", label: "CAC" },
  { id: "outras", label: "Outras Receitas" },
  { id: "pontual", label: "Pontual" },
  { id: "pontual-creators", label: "Pontual · Creators" },
] as const;

export type Bp2026TabId = typeof BP2026_TABS[number]["id"];

export const BP2026_TAB_IDS: Bp2026TabId[] = BP2026_TABS.map((t) => t.id);

export function abasPermitidas(
  role: string | null | undefined,
  allowedBpTabs: string[] | null | undefined,
): Bp2026TabId[] {
  if (role === "admin") return [...BP2026_TAB_IDS];
  const set = new Set(allowedBpTabs ?? []);
  return BP2026_TAB_IDS.filter((id) => set.has(id));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run shared/bp2026-tabs.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add shared/bp2026-tabs.ts shared/bp2026-tabs.test.ts
git commit -m "feat(bp2026): fonte única das abas + helper abasPermitidas"
```

---

### Task 2: Coluna `allowedBpTabs` no schema, no User e na serialização (+ migração local/prod)

**Files:**
- Modify: `shared/schema.ts:19-29` (tabela `authUsers`)
- Modify: `server/auth/userDb.ts:5-15` (interface `User`) e `:320-332` (`dbUserToUser`)
- Modify: `client/src/contexts/AuthContext.tsx:5-15` (interface `User`)
- Create: `migrations/2026-06-26-auth-users-allowed-bp-tabs.sql`

**Interfaces:**
- Consumes: nada.
- Produces: `authUsers.allowedBpTabs` (Drizzle `text[]`), `User.allowedBpTabs: string[]` (server e client), coluna `cortex_core.auth_users.allowed_bp_tabs text[] default '{}'` no banco.

- [ ] **Step 1: Adicionar a coluna no schema Drizzle**

Em `shared/schema.ts`, dentro de `authUsers` (logo após a linha `allowedRoutes: text("allowed_routes").array(),`), adicionar:

```ts
  allowedBpTabs: text("allowed_bp_tabs").array(),
```

- [ ] **Step 2: Adicionar `allowedBpTabs` à interface `User` do servidor**

Em `server/auth/userDb.ts`, na interface `User` (após `allowedRoutes: string[];`), adicionar:

```ts
  allowedBpTabs: string[];
```

- [ ] **Step 3: Incluir `allowedBpTabs` em `dbUserToUser`**

Em `server/auth/userDb.ts`, na função `dbUserToUser` (após a linha `allowedRoutes: migrateAllowedRoutes(dbUser.allowedRoutes),`), adicionar:

```ts
    allowedBpTabs: dbUser.allowedBpTabs ?? [],
```

- [ ] **Step 4: Adicionar `allowedBpTabs` à interface `User` do client**

Em `client/src/contexts/AuthContext.tsx`, na interface `User` (após `allowedRoutes: string[];`), adicionar:

```ts
  allowedBpTabs: string[];
```

- [ ] **Step 5: Escrever a migration SQL**

```sql
-- migrations/2026-06-26-auth-users-allowed-bp-tabs.sql
-- Coluna de whitelist de abas do BP 2026 por usuário (vazio = nenhuma aba).
ALTER TABLE cortex_core.auth_users
  ADD COLUMN IF NOT EXISTS allowed_bp_tabs text[] NOT NULL DEFAULT '{}';
```

- [ ] **Step 6: Aplicar a migration no banco LOCAL**

Run (credenciais locais em `reference_databases.md` — `localhost/cortex_dev`):
```bash
psql "$DATABASE_URL_LOCAL" -f migrations/2026-06-26-auth-users-allowed-bp-tabs.sql
```
Expected: `ALTER TABLE`.
Verificar:
```bash
psql "$DATABASE_URL_LOCAL" -c "\d cortex_core.auth_users" | grep allowed_bp_tabs
```
Expected: linha mostrando `allowed_bp_tabs | text[]`.

- [ ] **Step 7: Aplicar a migration no banco de PRODUÇÃO**

Run (credenciais prod em `reference_databases.md` — GCP `34.95.249.110/dados_turbo`):
```bash
psql "$DATABASE_URL_PROD" -f migrations/2026-06-26-auth-users-allowed-bp-tabs.sql
```
Expected: `ALTER TABLE`. Confirmar com o mesmo `\d` do passo anterior contra prod.

- [ ] **Step 8: Verificar typecheck**

Run: `npm run check 2>&1 | grep -E "schema.ts|userDb.ts|AuthContext.tsx" || echo "sem erros nos arquivos alterados"`
Expected: `sem erros nos arquivos alterados` (a baseline de erros pré-existentes do projeto não conta).

- [ ] **Step 9: Commit**

```bash
git add shared/schema.ts server/auth/userDb.ts client/src/contexts/AuthContext.tsx migrations/2026-06-26-auth-users-allowed-bp-tabs.sql
git commit -m "feat(bp2026): coluna allowedBpTabs em auth_users (schema + User + migração local/prod)"
```

---

### Task 3: Catálogo métrica→aba (anti-spoof do drill)

**Files:**
- Create: `server/routes/bp2026.catalogo.ts`
- Test: `server/routes/bp2026.catalogo.test.ts`

**Interfaces:**
- Consumes: `Bp2026TabId` de `shared/bp2026-tabs.ts`.
- Produces: `abasDaMetrica(metrica: string): Bp2026TabId[]` — todas as abas em que a métrica pode aparecer (vazio = desconhecida). `metricaPertenceAAba(metrica: string, aba: string): boolean`.

Notas de modelagem (da investigação):
- Prefixos por aba: `revenue` = `mrr_<produto>|contratos_<produto>|aov_<produto>|churn_pct_<produto>|churn_rs_*` (produtos: performance, creators, social, gc, others); `funil` = `funil_*|contratos_vendidos_mrr|contratos_vendidos_pontual|aov_venda_mrr|aov_venda_pontual|reunioes|taxa_conversao`; `vendasProduto` = `vendas_mrr_*|vendas_pontual_*|contratos_vendidos_mrr_*|contratos_vendidos_pontual_*`; `capacity` = `cap_*|gestores_*|designers_*|necessidade_*|contratos_por_*|contas_por_*`; `sga` = `sga_*|beneficio_total_empresa`; `cac` = `cac_*`; `outras` = `or_*`; `pontual`/`pontual-creators` = `pontual_*`.
- `dre` e `metricas` têm métricas enumeradas (resumos). Elas têm prioridade sobre os prefixos para os ids exatos que listam (ex.: `cac` exato → `dre`; `cac_vendas` → `cac`).

- [ ] **Step 1: Write the failing test**

```ts
// server/routes/bp2026.catalogo.test.ts
import { describe, it, expect } from "vitest";
import { abasDaMetrica, metricaPertenceAAba } from "./bp2026.catalogo";

describe("abasDaMetrica", () => {
  it("detalhadas por prefixo", () => {
    expect(abasDaMetrica("cac_vendas")).toEqual(["cac"]);
    expect(abasDaMetrica("sga_uzk")).toEqual(["sga"]);
    expect(abasDaMetrica("or_demais")).toEqual(["outras"]);
    expect(abasDaMetrica("funil_vendas_mrr")).toEqual(["funil"]);
    expect(abasDaMetrica("cap_contratos_performance")).toEqual(["capacity"]);
    expect(abasDaMetrica("vendas_mrr_performance")).toEqual(["vendasProduto"]);
    expect(abasDaMetrica("mrr_performance")).toEqual(["revenue"]);
  });
  it("pontual aparece em pontual E pontual-creators", () => {
    expect(abasDaMetrica("pontual_entrega")).toEqual(["pontual", "pontual-creators"]);
    expect(abasDaMetrica("pontual_squad:Alpha")).toEqual(["pontual", "pontual-creators"]);
  });
  it("agregados de resumo ficam no DRE/Métricas, não na aba detalhada", () => {
    expect(abasDaMetrica("cac")).toEqual(["dre"]);
    expect(abasDaMetrica("sga")).toEqual(["dre"]);
    expect(abasDaMetrica("mrr_ativo")).toEqual(["dre"]);
    expect(abasDaMetrica("churn_mes")).toEqual(["metricas"]);
  });
  it("desconhecida → vazio", () => {
    expect(abasDaMetrica("xpto_qualquer")).toEqual([]);
  });
});

describe("metricaPertenceAAba (anti-spoof)", () => {
  it("bloqueia métrica de CAC pedida via aba dre", () => {
    expect(metricaPertenceAAba("cac_vendas", "dre")).toBe(false);
  });
  it("aceita métrica na sua aba", () => {
    expect(metricaPertenceAAba("cac_vendas", "cac")).toBe(true);
    expect(metricaPertenceAAba("pontual_entrega", "pontual-creators")).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run server/routes/bp2026.catalogo.test.ts`
Expected: FAIL — `Cannot find module './bp2026.catalogo'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// server/routes/bp2026.catalogo.ts
// Mapeia uma métrica do BP 2026 à(s) aba(s) em que ela pode aparecer.
// Usado para barrar o drill: o front diz a aba de origem e aqui validamos
// (anti-spoof) que a métrica realmente pertence àquela aba.
import { BP2026_TAB_IDS, type Bp2026TabId } from "../../shared/bp2026-tabs";

const PRODUTOS_REVENUE = ["performance", "creators", "social", "gc", "others"];

// Métricas-resumo que aparecem nas abas dre/metricas (têm prioridade sobre prefixos).
const METRICAS_DRE = new Set<string>([
  "mrr_ativo", "receita_pontual", "outras_receitas", "inadimplencia", "impostos_receita",
  "csv_salarios", "csv_beneficio", "csv_stack", "cac", "sga", "bonus",
  "impostos_diretos", "capex", "dfc_real",
  "receita_total_faturavel", "receita_liquida", "margem_bruta", "ebitda", "geracao_caixa",
]);
const METRICAS_GERAIS = new Set<string>([
  "receita_total", "despesa_total", "vendas_mrr", "vendas_pontual", "colaboradores",
  "mrr_cabeca", "receita_cabeca", "clientes", "contratos", "ticket_cliente", "ticket_contrato",
  "churn_mes", "aliquota_efetiva", "margem_geracao", "saldo_caixa",
  "pessoas_csv", "pessoas_cac", "pessoas_sgea",
]);

function classificarPorPrefixo(m: string): Bp2026TabId | null {
  if (m.startsWith("pontual_")) return "pontual"; // tratado à parte (also creators)
  if (m.startsWith("vendas_mrr_") || m.startsWith("vendas_pontual_") ||
      m.startsWith("contratos_vendidos_mrr_") || m.startsWith("contratos_vendidos_pontual_")) return "vendasProduto";
  if (m === "funil_vendas_mrr" || m === "funil_vendas_pontual" ||
      m === "contratos_vendidos_mrr" || m === "contratos_vendidos_pontual" ||
      m === "aov_venda_mrr" || m === "aov_venda_pontual" ||
      m === "reunioes" || m === "taxa_conversao") return "funil";
  if (m.startsWith("cap_") || m.startsWith("gestores_") || m.startsWith("designers_") ||
      m.startsWith("necessidade_") || m.startsWith("contratos_por_") || m.startsWith("contas_por_")) return "capacity";
  if (m.startsWith("sga_") || m === "beneficio_total_empresa") return "sga";
  if (m.startsWith("cac_")) return "cac";
  if (m.startsWith("or_")) return "outras";
  // revenue: PREFIXO_<produto> e churn_rs_*
  for (const p of PRODUTOS_REVENUE) {
    if (m === `mrr_${p}` || m === `contratos_${p}` || m === `aov_${p}` ||
        m === `churn_pct_${p}` || m === `churn_rs_${p}`) return "revenue";
  }
  if (m === "churn_rs_total") return "revenue";
  return null;
}

export function abasDaMetrica(metrica: string): Bp2026TabId[] {
  if (metrica.startsWith("pontual_")) return ["pontual", "pontual-creators"];
  if (METRICAS_DRE.has(metrica)) return ["dre"];
  if (METRICAS_GERAIS.has(metrica)) return ["metricas"];
  const aba = classificarPorPrefixo(metrica);
  return aba ? [aba] : [];
}

export function metricaPertenceAAba(metrica: string, aba: string): boolean {
  return (abasDaMetrica(metrica) as string[]).includes(aba);
}

// Sanidade: garante que a lista de tabs do catálogo é a canônica.
void BP2026_TAB_IDS;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run server/routes/bp2026.catalogo.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/routes/bp2026.catalogo.ts server/routes/bp2026.catalogo.test.ts
git commit -m "feat(bp2026): catálogo métrica→aba para anti-spoof do drill"
```

---

### Task 4: Filtro do payload por aba (`/api/bp2026/receitas` e `/pontual-creators`)

**Files:**
- Create: `server/routes/bp2026.enforcement.ts`
- Test: `server/routes/bp2026.enforcement.test.ts`
- Modify: `server/routes/bp2026.ts:593-600` (handler `/receitas`) e `:602-615` (handler `/pontual-creators`)

**Interfaces:**
- Consumes: `abasPermitidas` de `shared/bp2026-tabs.ts`; `Bp2026TabId`.
- Produces: `filtrarPayloadPorAbas(payload, abas: Bp2026TabId[]): typeof payload` — devolve uma cópia sem as chaves cujas abas não estão em `abas`. `CHAVE_POR_ABA: Record<string,string>`.

Mapa aba→chave do payload: `dre→linhas, metricas→metricasGerais, revenue→revenue, funil→funil, vendasProduto→vendasProduto, capacity→capacity, sga→sgaDetalhe, cac→cacDetalhe, outras→outrasDetalhe, pontual→pontual`. (`pontual-creators` é endpoint separado.)

- [ ] **Step 1: Write the failing test**

```ts
// server/routes/bp2026.enforcement.test.ts
import { describe, it, expect } from "vitest";
import { filtrarPayloadPorAbas } from "./bp2026.enforcement";

const payload = {
  ano: 2026, mesCorrente: 6, mesFechado: 5,
  linhas: [1], metricasGerais: [2], revenue: [3], funil: [4], vendasProduto: [5],
  capacity: [6], sgaDetalhe: [7], cacDetalhe: [8], outrasDetalhe: [9], pontual: [10],
  atualizadoEm: "x",
};

describe("filtrarPayloadPorAbas", () => {
  it("mantém metadados e só as chaves das abas liberadas", () => {
    const r = filtrarPayloadPorAbas(payload as any, ["dre", "revenue"]);
    expect(r.linhas).toEqual([1]);
    expect(r.revenue).toEqual([3]);
    expect("cacDetalhe" in r).toBe(false);
    expect("sgaDetalhe" in r).toBe(false);
    expect(r.ano).toBe(2026);
    expect(r.mesCorrente).toBe(6);
  });
  it("nenhuma aba → só metadados", () => {
    const r = filtrarPayloadPorAbas(payload as any, []);
    expect("linhas" in r).toBe(false);
    expect("cacDetalhe" in r).toBe(false);
    expect(r.atualizadoEm).toBe("x");
  });
  it("não muta o payload original", () => {
    filtrarPayloadPorAbas(payload as any, ["dre"]);
    expect("cacDetalhe" in payload).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run server/routes/bp2026.enforcement.test.ts`
Expected: FAIL — módulo não existe.

- [ ] **Step 3: Write minimal implementation**

```ts
// server/routes/bp2026.enforcement.ts
// Reforço de confidencialidade do BP 2026: remove do payload as seções cujas
// abas o usuário não pode ver. Função pura (testável sem Express/DB).
import type { Bp2026TabId } from "../../shared/bp2026-tabs";

export const CHAVE_POR_ABA: Record<string, string> = {
  dre: "linhas",
  metricas: "metricasGerais",
  revenue: "revenue",
  funil: "funil",
  vendasProduto: "vendasProduto",
  capacity: "capacity",
  sga: "sgaDetalhe",
  cac: "cacDetalhe",
  outras: "outrasDetalhe",
  pontual: "pontual",
};

export function filtrarPayloadPorAbas<T extends Record<string, any>>(
  payload: T,
  abas: Bp2026TabId[],
): T {
  const permitidas = new Set<string>(abas);
  const out: Record<string, any> = { ...payload };
  for (const [aba, chave] of Object.entries(CHAVE_POR_ABA)) {
    if (!permitidas.has(aba)) delete out[chave];
  }
  return out as T;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run server/routes/bp2026.enforcement.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Aplicar o filtro no handler `/receitas`**

Em `server/routes/bp2026.ts`, no topo do arquivo (junto aos demais imports), adicionar:

```ts
import { abasPermitidas } from "../../shared/bp2026-tabs";
import { filtrarPayloadPorAbas } from "./bp2026.enforcement";
import type { User } from "../auth/userDb";
```

Substituir o handler atual (`:593-600`) por:

```ts
  app.get("/api/bp2026/receitas", async (req, res) => {
    try {
      const user = req.user as User;
      const abas = abasPermitidas(user?.role, user?.allowedBpTabs);
      const payload = await computarBpReceitas(db);
      res.json(filtrarPayloadPorAbas(payload, abas));
    } catch (error) {
      console.error("[bp2026] Erro em /api/bp2026/receitas:", error);
      res.status(500).json({ error: "Erro ao calcular orçado x realizado" });
    }
  });
```

- [ ] **Step 6: Barrar `/pontual-creators` sem a aba**

Em `server/routes/bp2026.ts`, no início do `try` do handler `/api/bp2026/pontual-creators` (`:602-615`), trocar a assinatura `(_req, res)` por `(req, res)` e adicionar, como primeira instrução dentro do `try`:

```ts
      const user = req.user as User;
      if (!abasPermitidas(user?.role, user?.allowedBpTabs).includes("pontual-creators")) {
        return res.status(403).json({ error: "Sem acesso a esta aba" });
      }
```

- [ ] **Step 7: Verificar typecheck**

Run: `npm run check 2>&1 | grep -E "bp2026\.ts|bp2026\.enforcement" || echo "sem erros nos arquivos alterados"`
Expected: `sem erros nos arquivos alterados`.

- [ ] **Step 8: Commit**

```bash
git add server/routes/bp2026.enforcement.ts server/routes/bp2026.enforcement.test.ts server/routes/bp2026.ts
git commit -m "feat(bp2026): filtrar payload de /receitas e barrar /pontual-creators por aba"
```

---

### Task 5: Enforcement do drill (`/detalhe` e `/reconciliacao`)

**Files:**
- Modify: `server/routes/bp2026.detalhe.ts:461-481` (handler `/detalhe`)
- Modify: `server/routes/bp2026.reconciliacao.ts:40-46` (handler `/reconciliacao`)

**Interfaces:**
- Consumes: `abasPermitidas` (`shared/bp2026-tabs.ts`), `metricaPertenceAAba` (`bp2026.catalogo.ts`), `User` (`server/auth/userDb`).
- Produces: nada novo (apenas gates).

- [ ] **Step 1: Imports no `/detalhe`**

Em `server/routes/bp2026.detalhe.ts`, junto aos imports do topo, adicionar:

```ts
import { abasPermitidas } from "../../shared/bp2026-tabs";
import { metricaPertenceAAba } from "./bp2026.catalogo";
import type { User } from "../auth/userDb";
```

- [ ] **Step 2: Gate de aba + anti-spoof no `/detalhe`**

Em `server/routes/bp2026.detalhe.ts`, dentro do handler (`app.get("/api/bp2026/detalhe", ...)`), logo após a leitura de `const metrica = String(req.query.metrica ?? "");` e antes do bloco de validação `if (!conhecida ...)`, inserir:

```ts
      const aba = String(req.query.aba ?? "");
      const user = req.user as User;
      const minhasAbas = abasPermitidas(user?.role, user?.allowedBpTabs);
      if (!minhasAbas.includes(aba as any)) {
        return res.status(403).json({ error: "Sem acesso a esta aba" });
      }
      if (!metricaPertenceAAba(metrica, aba)) {
        return res.status(400).json({ error: "métrica não pertence à aba" });
      }
```

- [ ] **Step 3: Gate de `revenue` no `/reconciliacao`**

Em `server/routes/bp2026.reconciliacao.ts`, adicionar os imports no topo:

```ts
import { abasPermitidas } from "../../shared/bp2026-tabs";
import type { User } from "../auth/userDb";
```

E dentro do handler, logo após `const mes = Number(req.query.mes);` e antes do `if (!PRODUTOS.includes(produto) ...)`, inserir:

```ts
      const user = req.user as User;
      if (!abasPermitidas(user?.role, user?.allowedBpTabs).includes("revenue")) {
        return res.status(403).json({ error: "Sem acesso a esta aba" });
      }
```

- [ ] **Step 4: Verificar typecheck**

Run: `npm run check 2>&1 | grep -E "bp2026\.detalhe|bp2026\.reconciliacao" || echo "sem erros nos arquivos alterados"`
Expected: `sem erros nos arquivos alterados`.

- [ ] **Step 5: Smoke test manual dos gates (sem sessão)**

Run (servidor de dev rodando):
```bash
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/bp2026/detalhe?metrica=cac_vendas&mes=3&aba=cac"
```
Expected: `401` (sem cookie de sessão, o middleware global barra antes). Isso só confirma que a rota está registrada; o teste real do gate de aba é feito no browser autenticado na Task 8.

- [ ] **Step 6: Commit**

```bash
git add server/routes/bp2026.detalhe.ts server/routes/bp2026.reconciliacao.ts
git commit -m "feat(bp2026): barrar drill (/detalhe, /reconciliacao) por aba + anti-spoof"
```

---

### Task 6: Frontend — esconder abas não liberadas e injetar `aba` nos drills

**Files:**
- Modify: `client/src/pages/BP2026.tsx` (render das tabs, estados de drill, props do detalhe)
- Modify: `client/src/components/bp2026/BPCellDetail.tsx:86-101` (prop `aba` na query)

**Interfaces:**
- Consumes: `abasPermitidas`, `BP2026_TABS` (`shared/bp2026-tabs.ts`); `useAuth` (`client/src/contexts/AuthContext`).
- Produces: UI que só mostra abas liberadas; drills enviando `?aba=<id>`.

- [ ] **Step 1: `BPCellDetail` aceita e envia `aba`**

Em `client/src/components/bp2026/BPCellDetail.tsx`, na interface `Props` (`:86-92`), adicionar `aba?: string;`. Atualizar a desestruturação (`:94`) para incluir `aba`. Substituir a `queryKey` (`:99`) por:

```ts
    queryKey: ["/api/bp2026/detalhe", { metrica: metrica ?? "", mes: String(mes ?? ""), ...(aba ? { aba } : {}), ...(segmento ? { segmento } : {}) }],
```

- [ ] **Step 2: BP2026 lê o usuário e calcula as abas permitidas**

Em `client/src/pages/BP2026.tsx`, adicionar aos imports:

```ts
import { useAuth } from "@/contexts/AuthContext";
import { BP2026_TABS, abasPermitidas } from "../../shared/bp2026-tabs";
```

Dentro do componente `BP2026()`, após os `useQuery`, adicionar:

```ts
  const { user } = useAuth();
  const abas = abasPermitidas(user?.role, user?.allowedBpTabs);
  const podeVer = (id: string) => abas.includes(id as any);
```

- [ ] **Step 3: Estados de drill carregam a aba de origem**

Em `client/src/pages/BP2026.tsx`, trocar a tipagem dos estados (`:61` e `:63`) para incluir `aba`:

```ts
  const [detalhe, setDetalhe] = useState<{ metrica: string; mes: number; aba: string } | null>(null);
  const [detalheCreators, setDetalheCreators] = useState<{ metrica: string; mes: number; aba: string } | null>(null);
```

- [ ] **Step 4: Cada aba renderiza condicionalmente e passa sua própria `aba` no clique**

Em `client/src/pages/BP2026.tsx`:

- Substituir a `<TabsList>` estática por uma lista derivada de `BP2026_TABS`, escondendo as não permitidas:

```tsx
  <TabsList>
    {BP2026_TABS.filter((t) => podeVer(t.id)).map((t) => (
      <TabsTrigger key={t.id} value={t.id}>{t.label}</TabsTrigger>
    ))}
  </TabsList>
```

- Em cada `<TabsContent>`, envolver com a checagem `podeVer(...)` e incluir a aba no `onCellClick`. Exemplos (aplicar o mesmo padrão às 11 abas):

```tsx
  {podeVer("cac") && (
    <TabsContent value="cac" className="mt-4">
      <BPDreTable
        linhas={data.cacDetalhe}
        mesCorrente={data.mesCorrente}
        mesFechado={data.mesFechado}
        onCellClick={(metrica, mes) => setDetalhe({ metrica, mes, aba: "cac" })}
      />
    </TabsContent>
  )}
```

```tsx
  {podeVer("revenue") && (
    <TabsContent value="revenue" className="mt-4">
      <BPDreTable
        linhas={data.revenue}
        mesCorrente={data.mesCorrente}
        mesFechado={data.mesFechado}
        onCellClick={(metrica, mes) => {
          const prod = metrica.startsWith("mrr_") ? metrica.slice(4) : "";
          if (PRODUTOS_REVENUE.includes(prod)) {
            const titulo = data.revenue.find((l) => l.metrica === metrica)?.titulo ?? prod;
            setRecon({ produto: prod, mes, titulo });
          } else {
            setDetalhe({ metrica, mes, aba: "revenue" });
          }
        }}
      />
    </TabsContent>
  )}
```

Para a aba `dre` use `aba: "dre"`, para `metricas` use `aba: "metricas"`, etc. Para a aba `pontual-creators`, o clique chama `setDetalheCreators({ metrica, mes, aba: "pontual-creators" })`.

- [ ] **Step 5: `defaultValue` = primeira aba liberada + estado vazio**

Em `client/src/pages/BP2026.tsx`, trocar `<Tabs defaultValue="dre">` por:

```tsx
  <Tabs defaultValue={abas[0] ?? "dre"}>
```

E, logo após o cabeçalho (antes do `<Tabs>`), tratar o caso de nenhuma aba:

```tsx
  {abas.length === 0 && (
    <div className="p-4 text-sm text-gray-600 dark:text-zinc-400">
      Você não tem acesso a nenhuma aba do BP 2026. Fale com um administrador.
    </div>
  )}
```

Envolver o bloco `<Tabs>...</Tabs>` em `{abas.length > 0 && ( ... )}`.

- [ ] **Step 6: Passar `aba` aos componentes de detalhe**

Em `client/src/pages/BP2026.tsx`, onde o `<BPCellDetail ... />` do `detalhe` é renderizado, adicionar a prop `aba={detalhe?.aba}` (e, no de creators, `aba={detalheCreators?.aba}`). Ex.:

```tsx
  <BPCellDetail
    metrica={detalhe?.metrica ?? null}
    mes={detalhe?.mes ?? null}
    aba={detalhe?.aba}
    linhas={data.linhas /* ...conforme o existente */}
    onClose={() => setDetalhe(null)}
  />
```

(Manter as demais props exatamente como já estão no arquivo.)

- [ ] **Step 7: Verificar typecheck**

Run: `npm run check 2>&1 | grep -E "BP2026\.tsx|BPCellDetail" || echo "sem erros nos arquivos alterados"`
Expected: `sem erros nos arquivos alterados`.

- [ ] **Step 8: Commit**

```bash
git add client/src/pages/BP2026.tsx client/src/components/bp2026/BPCellDetail.tsx
git commit -m "feat(bp2026): front esconde abas não liberadas e envia aba de origem no drill"
```

---

### Task 7: Administração — endpoint + UI para liberar abas por pessoa

**Files:**
- Modify: `server/middleware/schemas.ts:24-30` (novo schema)
- Modify: `server/auth/userDb.ts` (novo `updateUserBpTabs`, espelhando `updateUserPermissions:353-370`)
- Modify: `server/routes.ts` (novo handler `POST /api/users/:userId/bp-tabs`, junto aos `:528-562`)
- Modify: `client/src/pages/AdminUsuarios.tsx` (seção de checkboxes de abas no diálogo de permissões)

**Interfaces:**
- Consumes: `isAuthenticated`, `isAdmin`, `validateBody`; `BP2026_TABS`, `BP2026_TAB_IDS`.
- Produces: `updateBpTabsSchema`; `updateUserBpTabs(userId, allowedBpTabs): Promise<User|null>`; rota `POST /api/users/:userId/bp-tabs`.

- [ ] **Step 1: Schema de validação**

Em `server/middleware/schemas.ts`, após `updateRoleSchema` (`:30`), adicionar:

```ts
export const updateBpTabsSchema = z.object({
  allowedBpTabs: z.array(z.string()),
});
```

- [ ] **Step 2: Helper `updateUserBpTabs`**

Em `server/auth/userDb.ts`, após `updateUserPermissions` (`:370`), adicionar (espelha o existente):

```ts
export async function updateUserBpTabs(userId: string, allowedBpTabs: string[]): Promise<User | null> {
  try {
    const result = await db
      .update(authUsers)
      .set({ allowedBpTabs })
      .where(eq(authUsers.id, userId))
      .returning();

    if (result.length === 0) return null;

    const user = dbUserToUser(result[0]);
    setCachedUser(user);
    return user;
  } catch (error) {
    console.error("Erro ao atualizar abas do BP:", error);
    return null;
  }
}
```

- [ ] **Step 3: Endpoint admin**

Em `server/routes.ts`, no import de schemas (`:8`), acrescentar `updateBpTabsSchema`. Importar o helper `updateUserBpTabs` (junto de onde `updateUserPermissions` é importado). Adicionar o handler logo após o de `/permissions` (`:544`):

```ts
  app.post("/api/users/:userId/bp-tabs", isAuthenticated, isAdmin, validateBody(updateBpTabsSchema), async (req, res) => {
    try {
      const { userId } = req.params;
      const { allowedBpTabs } = req.body;
      const validos = allowedBpTabs.filter((t: string) => (BP2026_TAB_IDS as string[]).includes(t));
      const updatedUser = await updateUserBpTabs(userId, validos);
      if (!updatedUser) {
        return res.status(404).json({ error: "User not found" });
      }
      res.json(updatedUser);
    } catch (error) {
      console.error("[api] Error updating bp-tabs:", error);
      res.status(500).json({ error: "Failed to update bp-tabs" });
    }
  });
```

Adicionar ao topo de `server/routes.ts`: `import { BP2026_TAB_IDS } from "../shared/bp2026-tabs";` (ajustar o caminho relativo conforme a localização do arquivo).

- [ ] **Step 4: UI — mutation e seção de checkboxes**

Em `client/src/pages/AdminUsuarios.tsx`, dentro do `EditPermissionsDialog`, ao lado de `updatePermissionsMutation` (`:200-203`), adicionar estado e mutation para abas (importar `BP2026_TABS` de `../../shared/bp2026-tabs`):

```ts
  const [selectedBpTabs, setSelectedBpTabs] = useState<string[]>(user.allowedBpTabs ?? []);
  const updateBpTabsMutation = useMutation({
    mutationFn: async (allowedBpTabs: string[]) => {
      return await apiRequest('POST', `/api/users/${user.id}/bp-tabs`, { allowedBpTabs });
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['/api/users'] });
    },
  });
  const toggleBpTab = (id: string) =>
    setSelectedBpTabs((prev) => prev.includes(id) ? prev.filter((x) => x !== id) : [...prev, id]);
```

Adicionar uma seção de checkboxes (no mesmo container das categorias, seguindo o padrão de `:391-430`):

```tsx
  <div>
    <h4 className="text-sm font-semibold text-muted-foreground mb-3">Abas do BP 2026 (Orçado × Realizado)</h4>
    <div className="grid grid-cols-2 gap-3">
      {BP2026_TABS.map((t) => (
        <div key={t.id} className="flex items-center space-x-2">
          <Checkbox
            id={`bptab-${t.id}`}
            checked={selectedBpTabs.includes(t.id)}
            onCheckedChange={() => toggleBpTab(t.id)}
            data-testid={`checkbox-bptab-${t.id}`}
          />
          <Label htmlFor={`bptab-${t.id}`} className="text-sm font-normal cursor-pointer">{t.label}</Label>
        </div>
      ))}
    </div>
  </div>
```

No handler de "Salvar" do diálogo (onde `updatePermissionsMutation.mutate(selectedRoutes)` é chamado), disparar também `updateBpTabsMutation.mutate(selectedBpTabs)`.

- [ ] **Step 5: Verificar typecheck**

Run: `npm run check 2>&1 | grep -E "schemas\.ts|userDb\.ts|routes\.ts|AdminUsuarios" || echo "sem erros nos arquivos alterados"`
Expected: `sem erros nos arquivos alterados`.

- [ ] **Step 6: Commit**

```bash
git add server/middleware/schemas.ts server/auth/userDb.ts server/routes.ts client/src/pages/AdminUsuarios.tsx
git commit -m "feat(bp2026): admin libera abas por pessoa (endpoint + UI)"
```

---

### Task 8: Seed de rollout + verificação ponta-a-ponta

**Files:**
- Create: `migrations/2026-06-26-seed-bp-tabs.sql`

**Interfaces:**
- Consumes: coluna `allowed_bp_tabs`.
- Produces: usuários iniciais com acesso preservado.

- [ ] **Step 1: Confirmar com o usuário a lista do dia 1**

Antes de rodar: confirmar quais e-mails **não-admin** devem manter acesso na virada e a **quais abas** (admins já têm bypass automático). Preencher a lista abaixo com o que for confirmado.

- [ ] **Step 2: Escrever o seed**

```sql
-- migrations/2026-06-26-seed-bp-tabs.sql
-- Libera todas as 11 abas para os usuários não-admin que mantêm acesso no dia 1.
-- Substituir a lista de e-mails pela confirmada no Step 1.
UPDATE cortex_core.auth_users
SET allowed_bp_tabs = ARRAY[
  'dre','metricas','revenue','funil','vendasProduto','capacity',
  'sga','cac','outras','pontual','pontual-creators'
]
WHERE email IN (
  -- 'fulano@turbopartners.com.br',
  -- 'beltrano@turbopartners.com.br'
);
```

- [ ] **Step 3: Aplicar o seed em local e prod**

```bash
psql "$DATABASE_URL_LOCAL" -f migrations/2026-06-26-seed-bp-tabs.sql
psql "$DATABASE_URL_PROD"  -f migrations/2026-06-26-seed-bp-tabs.sql
```
Expected: `UPDATE <n>` em cada (n = nº de e-mails da lista).

- [ ] **Step 4: Rodar a suíte de testes da feature**

Run:
```bash
npx vitest run shared/bp2026-tabs.test.ts server/routes/bp2026.catalogo.test.ts server/routes/bp2026.enforcement.test.ts
```
Expected: todos PASS.

- [ ] **Step 5: Verificação manual no browser (reiniciar o dev server do BP backend)**

Com um usuário **não-admin** de teste liberado só para `dre` e `revenue`:
1. Abrir `/bp-2026`: só aparecem as abas Overview e Revenue.
2. Network: a resposta de `GET /api/bp2026/receitas` **não contém** `cacDetalhe`, `sgaDetalhe`, etc.
3. Drill numa célula de Revenue (mrr_*) abre a reconciliação normalmente.
4. Forjar `GET /api/bp2026/detalhe?metrica=cac_vendas&mes=3&aba=dre` no console autenticado → `400/403`.
5. Como **admin**: as 11 abas aparecem e o payload vem completo.
6. Conferir dark e light mode da página e do diálogo de admin.

- [ ] **Step 6: Commit**

```bash
git add migrations/2026-06-26-seed-bp-tabs.sql
git commit -m "chore(bp2026): seed de abas para os usuários do dia 1"
```

---

## Self-Review (preenchido)

**Cobertura do spec:** §1 Modelo de dados → Tasks 1, 2, 3. §2 Backend enforcement → Tasks 4, 5. §3 Frontend → Task 6. §4 Admin → Task 7. §5 Migração/seed → Tasks 2, 8. §6 Testes → testes em 1, 3, 4 + verificação manual em 8. Abas-resumo sensíveis → modeladas no catálogo (Task 3) e na whitelist (sem exceção para dre/metricas). Admin bypass → `abasPermitidas` (Task 1), usado em todo enforcement.

**Sem placeholders:** todo passo de código mostra o código. A única lista deixada em aberto (e-mails do seed, Task 8) depende de decisão operacional do usuário e está marcada como passo de confirmação explícito.

**Consistência de tipos:** `abasPermitidas(role, allowedBpTabs)`, `abasDaMetrica`/`metricaPertenceAAba`, `filtrarPayloadPorAbas`, `updateUserBpTabs`, `allowedBpTabs` — nomes e assinaturas iguais em todas as tasks que os consomem.
