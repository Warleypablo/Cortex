# BP 2026 Parte 13 (sub-aba CAC) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Oitava sub-aba "CAC": detalhamento do custo de aquisição por sub-linha, com drill, no padrão SG&A.

**Architecture:** Seed +9; `PREDICADOS_CAC_SUB`; `montarDetalhamentos` devolve `cac`; detalhe roteia sub-linhas para bucket e total para DERIVADAS; 8ª tab.

**Spec:** `docs/superpowers/specs/2026-06-10-bp2026-parte13-cac-design.md` (predicados, anti-drift, notas — parte deste plano).

**Contexto:** worktree `/Users/mac0267/Cortex/.claude/worktrees/bp2026-metricas-gerais`, branch `feature/bp2026-metricas-gerais` (PR #248).

---

### Task 1: Seed + API + detalhe (uma task — mudanças pequenas e acopladas)

**Files:**
- Modify: `scripts/seed-bp2026-orcado.py`
- Modify: `server/routes/bp2026.predicados.ts`
- Modify: `server/routes/bp2026.detalhamentos.ts`
- Modify: `server/routes/bp2026.ts`
- Modify: `server/routes/bp2026.detalhe.ts`

- [ ] **Step 1: Seed.** Em `LINHAS` (após as entradas de Outras Receitas):

```python
    ("CAC", 7, "cac_pre_vendas"),
    ("CAC", 8, "cac_vendas"),
    ("CAC", 9, "cac_gerencia"),
    ("CAC", 10, "cac_comissoes"),
    ("CAC", 11, "cac_growth"),
    ("CAC", 12, "cac_ads"),
    ("CAC", 13, "cac_eventos"),
    ("CAC", 14, "cac_brindes"),
    ("CAC", 15, "cac_viagens"),
```

Em `TOTAIS_ESPERADOS`:

```python
    "cac_pre_vendas": 439200.0,
    "cac_vendas": 410600.0,
    "cac_gerencia": 255000.0,
    "cac_comissoes": 953313.70653,
    "cac_growth": 396000.0,
    "cac_ads": 1360000.0,
    "cac_eventos": 380000.0,
    "cac_brindes": 195000.0,
    "cac_viagens": 60000.0,
```

Rodar e aplicar local+prod (comandos padrão; cwd /Users/mac0267/Cortex). Expected: `OK: 1066 statements` (82 DELETEs + 984 INSERTs). Spot-check jan: cac_comissoes ≈ 63745.709480, cac_ads 80000.

- [ ] **Step 2: Predicados.** Em `bp2026.predicados.ts`, após PREDICADOS_OUTRAS_SUB:

```typescript
// Sub-linhas do detalhamento CAC (bloco esquerdo da aba CAC da planilha).
// União = predicado cac do DRE para as categorias existentes hoje; categorias
// novas dentro de 06.04/06.06/06.07 fora das enumeradas apareceriam como
// divergência no smoke (cac_total_detalhe ≡ célula cac).
export const PREDICADOS_CAC_SUB: Record<string, SQL> = {
  cac_pre_vendas: sql`categoria_nome LIKE '06.04.03%'`,
  cac_vendas: sql`categoria_nome LIKE '06.04.02%'`,
  cac_gerencia: sql`categoria_nome LIKE '06.04.01%'`,
  cac_comissoes: sql`categoria_nome LIKE '06.04.04%' OR categoria_nome LIKE '06.04.05%'`,
  cac_growth: sql`categoria_nome LIKE '06.06.02%'`,
  cac_ads: sql`categoria_nome LIKE '06.06.01%'`,
  cac_eventos: sql`categoria_nome LIKE '06.07.01%'`,
  cac_brindes: sql`categoria_nome LIKE '06.07.02%'`,
  cac_viagens: sql`categoria_nome LIKE '05.04.02%' OR categoria_nome LIKE '06.05.04%'`,
  cac_outras_sub: sql`categoria_nome LIKE '06.05.05%' OR categoria_nome LIKE '06.07.03%'`,
};
```

- [ ] **Step 3: detalhamentos.** Em `bp2026.detalhamentos.ts`: importar `PREDICADOS_CAC_SUB`; adicionar as defs e o bloco (mesma mecânica do SG&A):

```typescript
const NOTA_CAC_TOTAL =
  "Soma das sub-linhas comerciais — deve bater com a linha CAC do DRE " +
  "(mesmos prefixos de categoria).";
const NOTA_COMISSOES = "Inclui Indique e Ganhe (06.04.05).";
const NOTA_CAC_OUTRAS =
  "Categorias que entram no CAC do DRE mas não têm linha no BP " +
  "(Outras Despesas Comerciais, Patrocínios).";

const SUB_CAC: { metrica: string; titulo: string; predicado: keyof typeof PREDICADOS_CAC_SUB; nota?: string; semOrcado?: boolean }[] = [
  { metrica: "cac_pre_vendas", titulo: "Pré Vendas", predicado: "cac_pre_vendas" },
  { metrica: "cac_vendas", titulo: "Vendas", predicado: "cac_vendas" },
  { metrica: "cac_gerencia", titulo: "Gerência", predicado: "cac_gerencia" },
  { metrica: "cac_comissoes", titulo: "Comissões", predicado: "cac_comissoes", nota: NOTA_COMISSOES },
  { metrica: "cac_growth", titulo: "Growth", predicado: "cac_growth" },
  { metrica: "cac_ads", titulo: "ADs", predicado: "cac_ads" },
  { metrica: "cac_eventos", titulo: "Eventos", predicado: "cac_eventos" },
  { metrica: "cac_brindes", titulo: "Brindes", predicado: "cac_brindes" },
  { metrica: "cac_viagens", titulo: "Viagens", predicado: "cac_viagens" },
  { metrica: "cac_outras_sub", titulo: "Outras comerciais (não orçadas)", predicado: "cac_outras_sub", nota: NOTA_CAC_OUTRAS, semOrcado: true },
];
```

No corpo de `montarDetalhamentos` (após o bloco SG&A, antes do de Outras): loop idêntico ao do SG&A construindo `cacLinhas`/`cacSeries` com `somaDespesaCaixaPorMes(db, PREDICADOS_CAC_SUB[def.predicado])` e `orcadoMes = (m) => def.semOrcado ? 0 : orcado[def.metrica]?.[m] ?? 0`; total:

```typescript
  const cacTotal = fazLinha(
    { metrica: "cac_total_detalhe", titulo: "CAC (soma das sub-linhas)", direcao: "menor_melhor", nota: NOTA_CAC_TOTAL, destaque: true },
    somaSeries(cacSeries),
    (m) => SUB_CAC.reduce((acc, d) => acc + (d.semOrcado ? 0 : orcado[d.metrica]?.[m] ?? 0), 0)
  );
```

Retorno passa a `{ sga, cac: [cacTotal, ...cacLinhas], outrasReceitas }`.

- [ ] **Step 4: bp2026.ts.** Destructuring ganha `cac: cacDetalhe`; payload ganha `cacDetalhe`.

- [ ] **Step 5: detalhe.** Em `bp2026.detalhe.ts`: import `PREDICADOS_CAC_SUB`; `DERIVADAS` += `"cac_total_detalhe"`; `TITULOS_SUBABAS` += as 10 sub-linhas (títulos da tabela acima); branch novo (junto do SGA_SUB):

```typescript
      } else if (Object.hasOwn(PREDICADOS_CAC_SUB, metrica)) {
        const itens = await itensDespesaBucket(db, PREDICADOS_CAC_SUB[metrica], mes);
        grupos = agruparItens(itens, LIMITE_ITENS);
        realizado = itens.reduce((s, i) => s + i.valor, 0);
```

(ATENÇÃO: `cac` do DRE já tem branch via METRICAS_BUCKET — não tocar.)

- [ ] **Step 6: Smoke** (porta 3970, DELETAR): cacDetalhe 11 linhas; INVARIANTE: cac_total_detalhe.meses ≡ cac do DRE (meses 1..mesCorrente, tolerância 0.01 — se divergir, reportar mês e diff); detalhe de cac_ads mes=5 ≡ célula; cac_total_detalhe → 400; orçado total jan ≈ 262495.71; cac_outras_sub orçado 0 com realizado > 0 em algum mês. tsc grep bp2026 vazio; vitest 14/14.

- [ ] **Step 7: Commit** — `feat(bp2026): detalhamento do CAC por sub-linha com drill`

---

### Task 2: Frontend

**Files:**
- Modify: `client/src/pages/BP2026.tsx`
- Modify: `client/src/components/bp2026/BPCellDetail.tsx`

- [ ] `ReceitasResponse` += `cacDetalhe: BPLinha[];`; tab `value="cac"` "CAC" entre SG&A e Outras Receitas (TabsTrigger + TabsContent com onCellClick, padrão das demais); `cacDetalhe` na concatenação do BPCellDetail; DERIVADAS client += `cac_total_detalhe: ["cac_pre_vendas", "cac_vendas", "cac_gerencia", "cac_comissoes", "cac_growth", "cac_ads", "cac_eventos", "cac_brindes", "cac_viagens", "cac_outras_sub"]`.
- [ ] tsc grep vazio; dev server; curl 200; visual do controller (tab, cores menor_melhor, drill numa sub-linha e no total). Commit — `feat(bp2026): oitava sub-aba cac com drill`

---

### Task 3: Final

- [ ] `npx vitest run`; push; PR #248 título "partes 7-13" + seção CAC via REST (se api.github.com falhar por DNS, usar `curl --resolve api.github.com:443:140.82.112.6` com `gh auth token`).
